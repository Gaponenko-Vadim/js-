# Руководство по созданию лекций

## 🎯 Главные правила

### 1. Одна лекция = один тест

- Каждая лекция покрывает вопросы **одного конкретного теста**
- ❌ Неправильно: глобальная лекция "HTTP методы"
- ✅ Правильно: лекция для теста "HTTP методы: основы" (20 вопросов)

### 2. Полное покрытие вопросов

**Лекция ОБЯЗАТЕЛЬНО должна давать знания для ответов на ВСЕ вопросы теста**

- Каждая концепция из вопросов детально объяснена
- Студент после прочтения уверенно отвечает на все вопросы
- Если студент не может ответить - лекция неполная!

### 3. Не давать прямые ответы

**Лекция объясняет концепции, а НЕ копирует вопросы из теста**

❌ **Неправильно**:

```markdown
Вопрос: Какой код при таймауте gateway?
Ответ: 504 Gateway Timeout
```

✅ **Правильно**:

```markdown
## 504 Gateway Timeout

Когда прокси-сервер не получает ответ от backend вовремя.

**Сценарий**: Nginx → backend (не отвечает 60 сек) → 504

> Разница от 502: 502 = backend недоступен, 504 = медленно отвечает
```

**Принципы**:

- Объясняйте концепцию через практические сценарии
- Формулировка отличается от вопроса в тесте
- Показывайте различия между похожими случаями

---

## Структура лекции

### 1. Введение (10-15%)

```markdown
# [Название темы]

## Введение

[Простая аналогия из реальной жизни]
```

**Примеры аналогий**: HTTP запросы = письма по почте, REST API = библиотека

### 2. Базовые концепции (15-20%)

```markdown
## Базовые концепции

[Таблица классификации]
[Общие принципы]
```

### 3. Детальный разбор (50-60%)

```markdown
## [Элемент] — [Описание]

> Определение простым языком

### Когда использовать

- Случай 1, 2, 3

### Особенности

✅ Что делать | ❌ Чего избегать | ⚠️ На что обратить внимание

### Примеры

\`\`\`http
[HTTP запрос/ответ с объяснением]
\`\`\`
```

**Требования**: минимум 2-3 примера, реальные API endpoints, edge cases

### 4. Сравнительные таблицы (5-10%)

Быстрое сравнение похожих элементов (GET vs POST, 200 vs 201)

### 5. Практические сценарии (10-15%)

```markdown
## Практические сценарии

### Сценарий 1: [Ситуация]

**Решение**: [код]
**Объяснение**: [почему так]

> 💡 Ключевой вывод
```

**Требования**:

- Минимум 8-10 сценариев
- Покрывают концепции из теста
- НЕ копируют вопросы буквально
- Объясняют "почему" и "как"

### 6. Типичные ошибки (5-10%)

```markdown
## Типичные ошибки

### Ошибка 1: [Название]

❌ **Неправильно**: [код]
**Проблема**: [последствия]
✅ **Правильно**: [код]

> 💡 Совет
```

**Требования**: минимум 5-8 ошибок, покрывают концепции из теста

### 7. Best Practices (5-10%)

Что ДЕЛАТЬ (в отличие от ошибок - что НЕ делать)

### 8. Заключение (3-5%)

Краткое резюме и 3 ключевых пункта

---

## Стиль написания

✅ **Используем**:

- Простые предложения, активный залог
- Разговорный тон, обращение "вы"
- Аналогии из реальной жизни
- Короткие абзацы (4-5 строк макс)
- **ОБЯЗАТЕЛЬНО**: Английские термины с переводом в скобках при первом упоминании

❌ **Избегаем**:

- Академический язык, пассивный залог
- Жаргон без объяснений
- Английские термины без перевода

**Эмодзи для навигации**: ✅ правильно | ❌ неправильно | ⚠️ важно | 💡 совет | 🎯 главное

**Форматирование**:

- `# H1` — название лекции
- `## H2` — разделы
- `### H3` — подразделы
- **Жирный** — термины
- `` `код` `` — inline код
- ` ```http ` — блоки кода

**Правило перевода английских терминов**:

✅ **Правильно** (при первом упоминании):

```markdown
**User Story (пользовательская история)** — краткое описание функциональности.
**Acceptance Criteria (критерии приемки)** определяют условия завершения.
**Main Flow (основной сценарий)** описывает happy path (счастливый путь).
```

❌ **Неправильно** (без перевода):

```markdown
User Story — краткое описание функциональности.
Acceptance Criteria определяют условия завершения.
Main Flow описывает happy path.
```

⚠️ **Важно**:

- Перевод в скобках **только при первом упоминании** термина
- При повторном использовании можно писать без перевода
- Устоявшиеся термины (API, HTTP, REST) можно без перевода
- Жаргонные выражения (happy path, scope creep) — обязательно с переводом

**Примеры повторного использования**:

```markdown
Первое упоминание: Sprint Backlog (бэклог спринта) содержит User Stories.
Повторное: Sprint Backlog формируется на Sprint Planning.
```

---

## Технический процесс

### 1. Найти тест

```typescript
const test = await prisma.test.findFirst({
  where: { title: "Название теста" },
  include: {
    questions: {
      include: { question: true },
      orderBy: { order: "asc" },
    },
  },
});
```

### 2. Проанализировать вопросы

**ОБЯЗАТЕЛЬНО изучите КАЖДЫЙ вопрос и варианты ответов**

Для каждого вопроса:

- Какая концепция объясняется?
- Почему правильный ответ правильный?
- Почему неправильные ответы неправильные?

**Создайте список всех концепций** → убедитесь, что лекция покроет каждую

### 3. Написать лекцию

Markdown контент 500-1000 строк по структуре выше

### 4. Создать в БД и привязать к вопросам

```typescript
const lecture = await prisma.lecture.create({
  data: {
    title: "Название",
    topic: "Тема",
    content: lectureContent,
  },
});

for (const questionId of test.questions.map((tq) => tq.questionId)) {
  await prisma.question.update({
    where: { id: questionId },
    data: { lectureId: lecture.id },
  });
}
```

**Важно**: Привязываем только к вопросам этого теста

---

## Контрольный чеклист

### Обязательно

- [ ] Проанализированы ВСЕ вопросы теста и варианты ответов
- [ ] Лекция даёт знания для ответов на ВСЕ вопросы
- [ ] Лекция НЕ копирует вопросы из теста буквально
- [ ] Концепции покрыты через практические сценарии

### Структура

- [ ] Введение с аналогией
- [ ] Базовые концепции с таблицами
- [ ] Детальный разбор элементов (2-3 примера каждый)
- [ ] Практические сценарии (8-10 штук)
- [ ] Типичные ошибки (5-8 штук)
- [ ] Best Practices
- [ ] Заключение

### Оформление

- [ ] Эмодзи для навигации
- [ ] Блоки кода, таблицы, цитаты
- [ ] Объём: 500-800 строк
- [ ] Привязка только к вопросам этого теста

---

## Шаблон скрипта

```typescript
import { PrismaClient } from "@prisma/client";
import { PrismaPg } from "@prisma/adapter-pg";
import { Pool } from "pg";
import dotenv from "dotenv";

dotenv.config();
const pool = new Pool({ connectionString: process.env.DATABASE_URL });
const adapter = new PrismaPg(pool);
const prisma = new PrismaClient({ adapter });

const lectureContent = `
# [Название лекции]

[Markdown контент лекции - 500-800 строк]
`;

async function createLecture() {
  try {
    // 1. Найти тест
    const test = await prisma.test.findFirst({
      where: { title: "[Название теста]" },
      include: {
        questions: {
          include: { question: true },
          orderBy: { order: "asc" },
        },
      },
    });

    if (!test) throw new Error("Тест не найден!");

    // 2. Создать лекцию
    const lecture = await prisma.lecture.create({
      data: {
        title: "[Название лекции]",
        topic: "[Тема]",
        content: lectureContent,
      },
    });

    // 3. Привязать к вопросам
    for (const q of test.questions) {
      await prisma.question.update({
        where: { id: q.questionId },
        data: { lectureId: lecture.id },
      });
    }

    console.log(
      "🎉 Лекция создана и привязана к",
      test.questions.length,
      "вопросам",
    );
  } catch (error) {
    console.error("❌ Ошибка:", error);
  } finally {
    await prisma.$disconnect();
  }
}

createLecture();
```

**Запуск**: `npx tsx scripts/create-[тема]-lecture.ts`

---

## Критерий качества

✅ **Хорошая лекция**:

- Студент уверенно отвечает на все вопросы теста после прочтения
- Понимает "почему" через объяснение концепций
- Может применить знания на практике
- Лекция объясняет концепции, а НЕ копирует вопросы

❌ **Плохая лекция**:

- Студент не может ответить на некоторые вопросы
- Лекция покрывает тему в общем, но не конкретные концепции
- Содержит буквальные копии вопросов из теста

> 💡 **Золотое правило**: Если студент не может ответить на вопрос теста после прочтения лекции — лекция требует доработки!

---

## Примеры хороших лекций

### Пример 1: User Stories (Пользовательские истории)

**Тест**: "User Stories: Написание пользовательских историй" (24 вопроса)
**Скрипт**: `scripts/create-user-stories-lecture-v3.ts`

**Что сделано правильно**:

✅ **Простое введение с аналогией**:

```markdown
## Введение: Планируем отпуск

Представьте: вы планируете отпуск с семьей.

**Вариант 1**: Написать 50 страниц подробного плана...
**Вариант 2**: Написать на стикере: "Хочу отдохнуть на море"...

User Stories — это второй вариант.
```

✅ **Короткие предложения** (10-15 слов максимум):

- "User Story — короткая карточка с описанием желания пользователя"
- "Всего 3 строки. Никаких технических деталей"

✅ **Практические примеры из жизни**:

- Интернет-магазин: фильтр по цене
- Блог: отложенная публикация
- Банковское приложение: SMS о транзакциях

✅ **8 детальных сценариев** с решениями и выводами

✅ **Сравнительные таблицы** для быстрого понимания:

```markdown
| Что    | User Story | Use Case     |
| ------ | ---------- | ------------ |
| Размер | 3 строки   | 5-10 страниц |
```

✅ **5 типичных ошибок** с исправлениями

✅ **Перевод терминов** при первом упоминании:

- "User Story (пользовательская история)"
- "Acceptance Criteria (критерии приемки)"
- "Epic (эпик)"

✅ **100% покрытие вопросов теста**:

- INVEST принципы
- Acceptance Criteria
- Epic vs Story
- As-Want-SoThat формат

**Объем**: ~800 строк
**Результат**: Студент может ответить на все 24 вопроса после прочтения

---

### Пример 2: Use Cases (Сценарии использования)

**Тест**: "Use Cases: Сценарии использования" (24 вопроса)
**Скрипт**: `scripts/create-use-cases-lecture-v2.ts`

**Что сделано правильно**:

✅ **Понятная аналогия во введении**:

```markdown
## Введение: Инструкция для банкомата

Банкомат работает по четкому сценарию:
Шаг 1: Вставьте карту
Шаг 2: Введите ПИН-код
...

Это и есть Use Case — пошаговое описание.
```

✅ **Четкая структура объяснений**:

- Что такое Use Case
- Чем отличается от User Story (таблица)
- Структура: Актор → Предусловия → Сценарии → Постусловия

✅ **Детальные примеры с альтернативами**:

```markdown
**Основной сценарий**:

1. Пользователь открывает форму входа
2. Система показывает поля Email и Пароль
   ...

**Альтернатива 5a**: Email не найден

- 5a.1. Система показывает "Неверный email или пароль"
- 5a.2. Возврат к шагу 3
```

✅ **8 практических сценариев**:

- Авторизация (с защитой от брутфорса)
- Создание поста в блоге
- Отмена заказа
- Добавление в корзину
- Восстановление пароля
- Модерация комментария
- Экспорт данных
- Подписка на рассылку

✅ **Объяснение Include vs Extend** простым языком:

- Include = обязательное действие (аналогия: тесто в рецепте пирога)
- Extend = опциональное (аналогия: украшения на торте)

✅ **5 типичных ошибок** с объяснением проблемы и решением

✅ **100% покрытие вопросов**:

- Структура Use Case
- Акторы
- Preconditions/Postconditions
- Main/Alternative/Exception flows
- Include/Extend

**Объем**: ~750 строк
**Результат**: Студент понимает как писать Use Cases и может ответить на все вопросы

---

## Ключевые принципы из примеров

### 1. Начинайте с простой аналогии

❌ **Плохо**: "Use Case — формализованное описание функциональных требований"
✅ **Хорошо**: "Use Case — как инструкция для банкомата: шаг 1, шаг 2..."

### 2. Используйте таблицы для сравнения

```markdown
| Что    | Вариант 1 | Вариант 2  |
| ------ | --------- | ---------- |
| Размер | 3 строки  | 10 страниц |
```

Таблица = быстрое понимание за 5 секунд

### 3. Примеры из реальной жизни

✅ Интернет-магазин, банкомат, блог, соцсеть
✅ Ситуации, с которыми сталкивался каждый

### 4. Короткие абзацы

❌ Абзац на 15 строк → студент запутается
✅ Абзац 3-5 строк → легко читать

### 5. Структура "Проблема → Решение → Вывод"

```markdown
**Ситуация**: Пользователи жалуются...
**User Story**: Я как... хочу... чтобы...
**Критерии приемки**: 1, 2, 3...

> 💡 **Вывод**: Решает проблему X
```

### 6. Минимум терминов, максимум смысла

❌ "Валидация осуществляется посредством..."
✅ "Система проверяет..."

### 7. Каждый пример должен учить

Не просто показать код, а объяснить:

- **Зачем** так сделано
- **Что** будет если сделать по-другому
- **Когда** использовать этот подход

---

## Шаблон упрощенной лекции

```markdown
# [Тема]: [Простое название]

## Введение: [Аналогия из жизни]

Представьте... [ситуация из реальной жизни]

[Тема] — это как [простое сравнение].

> 💡 **Главное**: [Ключевая мысль в 1 предложении]

---

## Что такое [Тема]

**[Термин]** — [определение простыми словами в 1 предложение].

### Пример из жизни

[Конкретный пример с 3-5 строками]

---

## [Основная концепция 1]

### [Подконцепция]

[Объяснение 2-3 предложения]

**Пример**:
[Код или текст примера]

**Зачем это нужно**: [Практическая выгода]

---

## Примеры vs Сравнения

| Что        | Вариант А  | Вариант Б  |
| ---------- | ---------- | ---------- |
| [Критерий] | [Значение] | [Значение] |

---

## Практические сценарии

### Сценарий 1: [Название ситуации]

**Ситуация**: [Описание проблемы]

**Решение**: [Код/текст решения]

**Почему так**: [Объяснение]

> 💡 **Вывод**: [Ключевой урок]

[Повторить для 7-8 сценариев]

---

## Типичные ошибки

### Ошибка 1: [Название]

❌ **Неправильно**: [Плохой пример]

**Проблема**: [Что не так]

✅ **Правильно**: [Исправленный пример]

> 💡 **Совет**: [Как избежать]

[Повторить для 5-8 ошибок]

---

## Best Practices

### 1. [Практика]

✅ [Что делать]
✅ [Почему это важно]

**Пример**: [Конкретный пример]

[Повторить для 5-7 практик]

---

## Заключение

[Тема] — [краткое резюме в 1-2 предложениях].

### Ключевые моменты

🎯 **[Пункт 1]**: [Объяснение]
🎯 **[Пункт 2]**: [Объяснение]
🎯 **[Пункт 3]**: [Объяснение]

[Финальная мысль]
```

---

## Как проверить качество лекции

### 1. Тест на бабушку

Прочитайте лекцию вслух. Если бабушка (без технического образования) поймет суть — лекция хорошая.

### 2. Тест на примеры

Каждая концепция должна иметь:

- ✅ Определение (что это)
- ✅ Аналогия (как это объяснить ребенку)
- ✅ Пример из жизни (где встречается)
- ✅ Код или сценарий (как применять)

### 3. Тест на вопросы

Откройте тест. Для каждого вопроса найдите раздел в лекции, который объясняет концепцию.

Если хотя бы 1 вопрос не покрыт → лекция неполная.

### 4. Тест на сложность

Подсчитайте средний размер предложения:

- ✅ 10-15 слов — отлично
- ⚠️ 15-20 слов — приемлемо
- ❌ 20+ слов — разбейте на несколько предложений

### 5. Тест на практичность

Минимум 8 практических сценариев с реальными примерами.
Не абстракции, а конкретные ситуации.

---

## Частые вопросы

### Вопрос: Сколько строк должна быть лекция?

**Ответ**: 500-800 строк. Меньше — не покроете все вопросы. Больше — студент устанет читать.

### Вопрос: Обязательно ли 8 сценариев?

**Ответ**: Минимум 8. Но если тест имеет 24 вопроса — возможно нужно 10-12 сценариев для полного покрытия.

### Вопрос: Что если концепция очень сложная?

**Ответ**: Разбейте на части:

1. Что это (простыми словами)
2. Зачем нужно (практическая выгода)
3. Как работает (пошагово)
4. Пример из жизни
5. Пример кода

### Вопрос: Можно ли использовать технические термины?

**Ответ**: Да, но:

1. При первом упоминании — с переводом
2. После первого раза — можно без перевода
3. Если термин сложный — дайте определение

### Вопрос: Как проверить что лекция отвечает на все вопросы?

**Ответ**:

1. Экспортируйте все вопросы теста
2. Для каждого вопроса найдите раздел в лекции
3. Прочитайте раздел — студент сможет ответить?
4. Если нет — добавьте детали или пример

---

## Полезные ссылки

- **User Stories лекция**: `scripts/create-user-stories-lecture-v3.ts`
- **Use Cases лекция**: `scripts/create-use-cases-lecture-v2.ts`
- **Чеклист качества**: `LECTURE_CHECKLIST.md`

> 💡 **Совет**: Изучите оба примера перед созданием новой лекции. Они показывают правильный уровень простоты и детализации.
