# Тестирование Email Верификации

## ✅ Что реализовано

### 1. Схема БД
- ✅ Поле `emailVerified` добавлено в модель User
- ✅ Модель `EmailVerificationToken` создана
- ✅ Существующие пользователи обновлены (emailVerified = now())
- ✅ Prisma Client регенерирован

### 2. Backend API
- ✅ `src/lib/email.ts` - утилита отправки email (консольные логи)
- ✅ `src/app/api/auth/verify-email/route.ts` - endpoint подтверждения email
- ✅ `src/app/api/register/route.ts` - создание токена и отправка письма
- ✅ `src/lib/auth.ts` - проверка emailVerified при входе

### 3. Frontend UI
- ✅ `LoginForm.tsx` - обработка URL параметров, info сообщения
- ✅ `RegisterForm.tsx` - редирект с сообщением после регистрации
- ✅ `AuthForm.module.scss` - стили для .info блока

---

## 🧪 Сценарии тестирования

### Сценарий 1: Новый пользователь регистрируется

**Шаги:**
1. Перейти на http://localhost:3000/register
2. Заполнить форму:
   - Имя: Test User
   - Email: test@example.com
   - Пароль: password123
   - Подтверждение: password123
3. Нажать "Зарегистрироваться"

**Ожидаемый результат:**
- ✅ Перенаправление на `/login?success=RegistrationSuccess&email=test@example.com`
- ✅ Синий блок с текстом: "Регистрация успешна! На email test@example.com отправлено письмо..."
- ✅ В консоли сервера появился лог с ссылкой подтверждения:
```
====================================
EMAIL ПОДТВЕРЖДЕНИЕ РЕГИСТРАЦИИ
====================================
Кому: test@example.com
Тема: Подтверждение email

Перейдите по ссылке для подтверждения вашего email:
http://localhost:3000/api/auth/verify-email?token=...

Ссылка действительна 24 часа.
====================================
```

**Проверка в БД (Prisma Studio):**
```bash
npx prisma studio
```
- В таблице `User` создан пользователь с `emailVerified = null`
- В таблице `email_verification_tokens` создан токен с expiresAt = +24 часа

---

### Сценарий 2: Попытка войти БЕЗ подтверждения email

**Шаги:**
1. Перейти на http://localhost:3000/login
2. Попытаться войти с test@example.com / password123

**Ожидаемый результат:**
- ❌ Красный блок с ошибкой: "Email не подтвержден. Проверьте почту и перейдите по ссылке из письма."
- ❌ Вход не выполнен

---

### Сценарий 3: Подтверждение email по ссылке

**Шаги:**
1. Скопировать ссылку из консоли сервера (из логов регистрации)
2. Вставить в браузер и перейти
   - Например: `http://localhost:3000/api/auth/verify-email?token=abc-123-xyz`

**Ожидаемый результат:**
- ✅ Перенаправление на `/login?success=EmailVerified`
- ✅ Синий блок: "Email успешно подтвержден! Теперь можете войти."
- ✅ В БД: User.emailVerified установлен = текущая дата
- ✅ Токен удален из таблицы `email_verification_tokens`

---

### Сценарий 4: Вход ПОСЛЕ подтверждения email

**Шаги:**
1. Войти с test@example.com / password123

**Ожидаемый результат:**
- ✅ Успешный вход
- ✅ Перенаправление на `/dashboard`

---

### Сценарий 5: Повторное подтверждение уже подтвержденного email

**Шаги:**
1. Повторно зарегистрировать пользователя с другим email
2. Подтвердить email по ссылке
3. Попытаться перейти по той же ссылке снова

**Ожидаемый результат:**
- ✅ Перенаправление на `/login?success=AlreadyVerified`
- ✅ Синий блок: "Email уже был подтвержден."

---

### Сценарий 6: Просроченный токен (Edge case)

**Как протестировать:**
1. Зарегистрировать пользователя
2. В БД вручную изменить `expiresAt` токена на прошедшую дату:
```sql
UPDATE email_verification_tokens
SET "expiresAt" = NOW() - INTERVAL '1 day'
WHERE token = 'ваш-токен';
```
3. Перейти по ссылке подтверждения

**Ожидаемый результат:**
- ❌ Перенаправление на `/login?error=TokenExpired`
- ❌ Красный блок: "Ссылка подтверждения истекла. Попробуйте зарегистрироваться заново."
- ✅ Токен удален из БД

---

### Сценарий 7: Недействительный токен

**Шаги:**
1. Перейти по ссылке с несуществующим токеном:
   - `http://localhost:3000/api/auth/verify-email?token=invalid-token-12345`

**Ожидаемый результат:**
- ❌ Перенаправление на `/login?error=TokenNotFound`
- ❌ Красный блок: "Недействительная ссылка подтверждения."

---

## 📊 Проверка в Prisma Studio

```bash
npx prisma studio
```

**Таблица User:**
- Поле `emailVerified`: DateTime | null
- Старые пользователи: emailVerified = дата обновления
- Новые (не подтвержденные): emailVerified = null
- Новые (подтвержденные): emailVerified = дата подтверждения

**Таблица email_verification_tokens:**
- Активные токены (expiresAt > now())
- После подтверждения токен удаляется

---

## 🐛 Известные ограничения

### 1. Email отправка - временная заглушка
**Текущая реализация:** Логи в консоль сервера

**TODO для production:**
- Интегрировать реальный email сервис (Resend, SendGrid, Nodemailer)
- Настроить SMTP
- Создать email шаблоны

**Код для интеграции находится в:** `src/lib/email.ts`

### 2. Повторная отправка письма
**Текущая реализация:** Нет функционала повторной отправки

**TODO для будущего:**
- Добавить кнопку "Отправить письмо повторно" на странице логина
- API endpoint для повторной отправки: `POST /api/auth/resend-verification`
- Ограничение: не чаще 1 раза в 5 минут

### 3. Очистка просроченных токенов
**Текущая реализация:** Токены удаляются только при попытке использования

**TODO для production:**
- Создать cron job для очистки просроченных токенов
- Запускать раз в день: `DELETE FROM email_verification_tokens WHERE expiresAt < NOW()`

---

## ✅ Checklist завершения

- [x] Схема БД обновлена и миграция применена
- [x] Prisma Client регенерирован
- [x] Существующие пользователи обновлены (emailVerified = now())
- [x] API endpoint для верификации создан
- [x] API register обновлен (создание токена + отправка письма)
- [x] NextAuth проверяет emailVerified при входе
- [x] UI обновлен (info сообщения, обработка URL параметров)
- [x] Стили для .info блока добавлены
- [ ] **TODO:** Протестировать все 7 сценариев вручную
- [ ] **TODO:** Интегрировать реальный email сервис (production)

---

## 🚀 Что делать дальше

### Для локального тестирования:
1. Запустить dev сервер: `npm run dev`
2. Открыть http://localhost:3000
3. Пройти сценарии тестирования выше
4. Проверить логи в консоли сервера
5. Проверить данные в Prisma Studio

### Для production:
1. Интегрировать email сервис (Resend рекомендуется)
2. Настроить переменные окружения:
```env
EMAIL_FROM="noreply@yourdomain.com"
RESEND_API_KEY="re_..."
```
3. Протестировать отправку реальных писем
4. Настроить cron job для очистки токенов
5. Добавить функционал повторной отправки письма

---

**Дата создания:** 2026-01-28
**Версия:** 1.0
**Статус:** ✅ Ready for Testing
