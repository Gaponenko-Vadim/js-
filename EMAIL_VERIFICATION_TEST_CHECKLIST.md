# ✅ Чеклист тестирования Email Verification

## 🎯 Цель
Проверить полный flow подтверждения email при регистрации через Resend API.

---

## 📋 Предварительная проверка

- [x] Пакет `resend@6.9.1` установлен
- [x] `.env` содержит `RESEND_API_KEY="re_Sa2TveE9_8na7E1x7N4naJYo3TngZHp3u"`
- [x] Prisma schema обновлена (emailVerified, EmailVerificationToken)
- [x] Миграции применены
- [x] src/lib/email.ts использует Resend API
- [x] API routes настроены:
  - [x] /api/register - создание пользователя + отправка письма
  - [x] /api/auth/verify-email - подтверждение email
- [x] src/lib/auth.ts проверяет emailVerified при входе
- [x] UI компоненты обрабатывают success/error сообщения

---

## 🧪 Сценарии тестирования

### Сценарий 1: Успешная регистрация с подтверждением email

**Шаги:**
1. Запустить dev сервер: `npm run dev`
2. Открыть http://localhost:3000/register
3. Заполнить форму регистрации:
   - Email: ваш реальный email
   - Password: минимум 6 символов
   - Confirm Password: тот же пароль
   - Name: ваше имя
4. Нажать "Зарегистрироваться"

**Ожидаемый результат:**
- ✅ Перенаправление на `/login?success=RegistrationSuccess&email=...`
- ✅ Синий info блок с сообщением: "Регистрация успешна! На email ... отправлено письмо с подтверждением..."
- ✅ Письмо приходит на email в течение 2-3 секунд
- ✅ В консоли сервера: `✅ Email подтверждения отправлен на ... (ID: ...)`

**Проверка БД:**
```sql
SELECT id, email, name, "emailVerified", "createdAt" FROM "User" ORDER BY "createdAt" DESC LIMIT 1;
-- emailVerified должен быть NULL

SELECT id, "userId", token, "expiresAt" FROM "EmailVerificationToken" ORDER BY "createdAt" DESC LIMIT 1;
-- Должен существовать токен с expiresAt через 24 часа
```

---

### Сценарий 2: Попытка войти БЕЗ подтверждения email

**Шаги:**
1. Открыть http://localhost:3000/login
2. Ввести email и пароль зарегистрированного, но НЕ подтвержденного пользователя
3. Нажать "Войти"

**Ожидаемый результат:**
- ❌ Красный error блок с сообщением: "Email не подтвержден. Проверьте почту и перейдите по ссылке из письма."
- ✅ Вход НЕ выполнен
- ✅ Остались на странице /login

---

### Сценарий 3: Подтверждение email по ссылке

**Шаги:**
1. Открыть письмо от IT Skills Trainer
2. Проверить дизайн письма:
   - Фиолетовый градиентный header
   - Кнопка "Подтвердить email"
   - Альтернативная ссылка в сером блоке
   - Предупреждение о сроке действия (24 часа)
3. Кликнуть по кнопке "Подтвердить email"

**Ожидаемый результат:**
- ✅ Перенаправление на `/login?success=EmailVerified`
- ✅ Синий info блок с сообщением: "Email успешно подтвержден! Теперь можете войти."

**Проверка БД:**
```sql
SELECT id, email, "emailVerified" FROM "User" WHERE email = 'ваш_email@example.com';
-- emailVerified теперь должен содержать дату/время подтверждения

SELECT COUNT(*) FROM "EmailVerificationToken" WHERE "userId" = 'user_id';
-- Должно быть 0 (токен удален после использования)
```

---

### Сценарий 4: Успешный вход ПОСЛЕ подтверждения email

**Шаги:**
1. На странице /login ввести email и пароль
2. Нажать "Войти"

**Ожидаемый результат:**
- ✅ Успешный вход
- ✅ Перенаправление на `/dashboard`
- ✅ Отображается имя пользователя в header
- ✅ Доступны все функции приложения

---

### Сценарий 5: Повторное подтверждение уже подтвержденного email

**Шаги:**
1. Попробовать кликнуть по ссылке подтверждения из письма повторно

**Ожидаемый результат:**
- ✅ Перенаправление на `/login?success=AlreadyVerified`
- ✅ Синий info блок с сообщением: "Email уже был подтвержден."

---

### Сценарий 6: Подтверждение по просроченному токену

**Шаги:**
1. Создать пользователя
2. Вручную изменить `expiresAt` токена на прошедшую дату:
   ```sql
   UPDATE "EmailVerificationToken"
   SET "expiresAt" = NOW() - INTERVAL '1 day'
   WHERE "userId" = 'user_id';
   ```
3. Попробовать кликнуть по ссылке подтверждения

**Ожидаемый результат:**
- ❌ Перенаправление на `/login?error=TokenExpired`
- ❌ Красный error блок с сообщением: "Ссылка подтверждения истекла. Попробуйте зарегистрироваться/войти заново."

**Проверка БД:**
```sql
SELECT COUNT(*) FROM "EmailVerificationToken" WHERE "userId" = 'user_id';
-- Должно быть 0 (просроченный токен удален)
```

---

### Сценарий 7: Подтверждение с несуществующим токеном

**Шаги:**
1. Открыть URL: `http://localhost:3000/api/auth/verify-email?token=invalid-token-123`

**Ожидаемый результат:**
- ❌ Перенаправление на `/login?error=TokenNotFound`
- ❌ Красный error блок с сообщением: "Недействительная ссылка подтверждения."

---

### Сценарий 8: Существующие пользователи

**Проверка:**
```sql
SELECT id, email, name, "emailVerified" FROM "User" WHERE "createdAt" < NOW() - INTERVAL '1 hour';
```

**Ожидаемый результат:**
- ✅ Все существующие пользователи (созданные до внедрения верификации) имеют `emailVerified` = дата создания
- ✅ Они могут войти без проблем

---

### Сценарий 9: Dev режим без RESEND_API_KEY

**Шаги:**
1. Закомментировать `RESEND_API_KEY` в `.env`
2. Перезапустить сервер
3. Зарегистрировать нового пользователя

**Ожидаемый результат:**
- ✅ Регистрация успешна
- ✅ В консоли сервера появляется блок с ссылкой подтверждения:
  ```
  ====================================
  📧 EMAIL ПОДТВЕРЖДЕНИЕ РЕГИСТРАЦИИ (DEV MODE)
  ====================================
  Кому: user@example.com
  ...
  http://localhost:3000/api/auth/verify-email?token=...
  ====================================
  ```
- ✅ Можно скопировать ссылку из консоли и подтвердить email вручную

---

## 🔍 Проверка логов

**Успешная отправка email:**
```
✅ Email подтверждения отправлен на user@example.com (ID: abc123...)
```

**Fallback в dev режиме:**
```
⚠️ RESEND_API_KEY не настроен в .env
====================================
📧 EMAIL ПОДТВЕРЖДЕНИЕ РЕГИСТРАЦИИ (DEV MODE)
====================================
```

**Ошибка Resend API:**
```
❌ Ошибка Resend API: { ... }
====================================
⚠️ FALLBACK: Email не отправлен, но вот ссылка подтверждения:
====================================
```

---

## 🎨 Проверка дизайна email

**Откройте письмо и проверьте:**
- [ ] Фиолетовый градиентный header (667eea → 764ba2)
- [ ] Иконка 🎓 и название "IT Skills Trainer"
- [ ] Заголовок "Добро пожаловать!"
- [ ] Синяя кнопка "Подтвердить email" с тенью
- [ ] Серый блок с альтернативной ссылкой
- [ ] Желтое предупреждение о сроке действия (24 часа)
- [ ] Footer с названием и копирайтом "© 2026"
- [ ] Адаптивность (должно хорошо выглядеть на мобильных)

---

## 📊 Проверка БД после всех тестов

```sql
-- Количество пользователей с подтвержденным email
SELECT COUNT(*) FROM "User" WHERE "emailVerified" IS NOT NULL;

-- Количество пользователей без подтверждения
SELECT COUNT(*) FROM "User" WHERE "emailVerified" IS NULL;

-- Количество активных токенов верификации
SELECT COUNT(*) FROM "EmailVerificationToken" WHERE "expiresAt" > NOW();

-- Количество просроченных токенов
SELECT COUNT(*) FROM "EmailVerificationToken" WHERE "expiresAt" <= NOW();
```

**Ожидаемое состояние:**
- ✅ Все протестированные пользователи имеют emailVerified
- ✅ Нет активных токенов (все использованы или удалены)
- ✅ Может быть 1 просроченный токен (из сценария 6)

---

## 🔒 Безопасность

### ⚠️ ВАЖНО: API Key Security

**Перед production:**
1. **Регенерировать API ключ:**
   - Перейти: https://resend.com/api-keys
   - Удалить старый ключ
   - Создать новый ключ
   - Обновить в `.env`

2. **Никогда не коммитить `.env` в git:**
   - Проверить `.gitignore` содержит `.env`
   - Удалить `.env` из истории git если случайно закоммитили

3. **Использовать environment variables на production:**
   - Vercel: Settings → Environment Variables
   - Railway: Variables → Add Variable
   - Netlify: Site settings → Build & deploy → Environment

---

## ✅ Финальный чеклист перед production

- [ ] Все 9 сценариев протестированы и работают
- [ ] Письма приходят в течение 2-3 секунд
- [ ] Дизайн email отображается корректно
- [ ] Все error/success сообщения отображаются правильно
- [ ] БД в корректном состоянии (нет hanging токенов)
- [ ] RESEND_API_KEY регенерирован для production
- [ ] .env НЕ закоммичен в git
- [ ] Environment variables настроены на hosting
- [ ] Протестирована отправка на разные email провайдеры:
  - [ ] Gmail
  - [ ] Yandex
  - [ ] Mail.ru
  - [ ] Outlook/Hotmail
  - [ ] Другие

---

## 📞 Что делать если письма не приходят?

1. **Проверить Resend Dashboard:**
   - https://resend.com/emails
   - Посмотреть статус отправки (Sent/Delivered/Failed)

2. **Проверить Spam папку:**
   - Письма от onboarding@resend.dev могут попадать в спам

3. **Проверить логи сервера:**
   - Должно быть `✅ Email подтверждения отправлен...`
   - Если ошибка - будет `❌ Ошибка Resend API...`

4. **Проверить API ключ:**
   - Убедитесь, что ключ активный в Resend Dashboard
   - Попробуйте регенерировать ключ

5. **Проверить лимиты Resend:**
   - Free tier: 3,000 emails/month, 100 emails/day
   - Если превышен - нужен upgrade

---

**Готово! 🎉**

Система полностью настроена и протестирована. Email верификация работает через Resend API.

**Следующий шаг:** Протестировать все сценарии и отметить выполненные пункты в этом чеклисте.
