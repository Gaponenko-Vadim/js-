# 📧 Альтернатива: Mail.ru SMTP

## Почему Mail.ru может работать

Mail.ru использует порт 465, но иногда его не блокируют провайдеры (в отличие от Yandex/Gmail).

---

## 🔧 Настройка Mail.ru

### Шаг 1: Получить/создать Mail.ru почту
- Есть Mail.ru → используйте его
- Нет → создайте: https://account.mail.ru/signup

### Шаг 2: Получить пароль приложения

1. Войти: https://account.mail.ru/user/2-step-auth/passwords/
2. Нажать "Создать пароль"
3. Название: "IT Skills Trainer"
4. Скопировать пароль

### Шаг 3: Обновить код

**В `src/lib/email.ts` изменить:**
```typescript
return nodemailer.createTransport({
  host: 'smtp.mail.ru', // Mail.ru SMTP
  port: 465, // SSL порт
  secure: true, // true для 465
  auth: {
    user: process.env.EMAIL_USER,
    pass: process.env.EMAIL_APP_PASSWORD,
  },
});
```

**В `.env` добавить:**
```env
EMAIL_USER="youremail@mail.ru"
EMAIL_APP_PASSWORD="ваш-пароль-приложения"
```

---

## 🎯 Вариант 4: Resend (Профессиональное решение)

**Для production рекомендую:**
- https://resend.com
- Работает через HTTP API (не SMTP!)
- НЕ блокируется провайдерами
- 3000 писем/мес бесплатно
- $20/мес для 50,000 писем

**Установка:**
```bash
npm install resend
```

**Код:**
```typescript
import { Resend } from 'resend';

const resend = new Resend(process.env.RESEND_API_KEY);

await resend.emails.send({
  from: 'noreply@yourdomain.com',
  to: email,
  subject: 'Подтверждение email',
  html: '...'
});
```

**Преимущества:**
- ✅ Работает без SMTP (через HTTPS)
- ✅ Не блокируется провайдерами
- ✅ Высокая доставляемость
- ✅ React Email компоненты
- ✅ Аналитика и логи

---

**Для разработки:** Используйте режим без email (ссылки в консоли)
**Для production:** Resend или VPN + Yandex
