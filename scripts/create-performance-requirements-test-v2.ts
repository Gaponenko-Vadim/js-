import { PrismaClient } from '@prisma/client';
import { PrismaPg } from '@prisma/adapter-pg';
import { Pool } from 'pg';
import dotenv from 'dotenv';
import path from 'path';

const envPath = path.resolve(__dirname, '..', '.env');
dotenv.config({ path: envPath });

const pool = new Pool({ connectionString: process.env.DATABASE_URL });
const adapter = new PrismaPg(pool);
const prisma = new PrismaClient({ adapter });

const lectureContent = `# Требования к производительности (Performance Requirements)

## Введение: Доставка пиццы и производительность

Представьте пиццерию, которая принимает заказы онлайн:

- **Быстрая доставка** = 15 минут → клиенты довольны, заказывают снова
- **Медленная доставка** = 2 часа → клиенты злятся, больше не заказывают
- **Много заказов одновременно** = 100 пицц в час → пиццерия справляется
- **Мало заказов** = 5 пицц в час → очередь растёт, клиенты уходят

**Требования к производительности** — это то же самое, но для вашего API:

> "Насколько **быстро** должна работать система и сколько **одновременных пользователей** она должна обслуживать"

**Ключевые вопросы:**
- Как быстро пользователи получают ответ? (Response Time = время доставки пиццы)
- Сколько запросов в секунду система выдерживает? (Throughput = сколько пицц в час)
- Сколько времени данные идут по сети? (Latency = задержка перед началом готовки)

---

## Что такое требования к производительности?

### Определение

> "Требования к производительности (Performance Requirements) — правила того, как **быстро** и **эффективно** система должна работать при заданной нагрузке"

### Ключевые аспекты

**1. Response Time (Время отклика)**
Как быстро система отвечает на запрос
> Аналогия: Сколько времени от заказа пиццы до её получения

**2. Throughput (Пропускная способность)**
Сколько операций система обрабатывает за единицу времени
> Аналогия: Сколько пицц пиццерия готовит за час

**3. Latency (Задержка)**
Время передачи данных по сети
> Аналогия: Задержка перед началом готовки пиццы

**4. Concurrency (Одновременность)**
Сколько пользователей система может обслуживать одновременно
> Аналогия: Сколько заказов пиццерия обрабатывает параллельно

---

## Response Time — Время отклика

### Что это?

> "Response Time — время от отправки запроса до получения полного ответа"

**Компоненты:**
\`\`\`
Total Response Time =
  Network Latency (туда)
  + Server Processing Time (обработка)
  + Database Query Time (запрос к БД)
  + Network Latency (обратно)
\`\`\`

**Аналогия с пиццей:**
\`\`\`
Общее время доставки =
  Курьер едет к пиццерии
  + Пицца готовится
  + Курьер едет к клиенту
\`\`\`

### Почему среднее время — плохая метрика?

❌ **Проблема со средним:**

\`\`\`
100 запросов:
- 90 запросов по 100ms
- 10 запросов по 5000ms

Average (среднее) = (90×100 + 10×5000) / 100 = 590ms
→ "Выглядит нормально!"

НО: 10% клиентов ждут 5 секунд — они уходят!
\`\`\`

✅ **Решение: Перцентили (p50, p95, p99)**

**p50 (медиана):**
- 50% запросов быстрее этого времени
- Показывает типичный опыт

**p95:**
- 95% запросов быстрее этого времени
- Показывает опыт большинства

**p99:**
- 99% запросов быстрее
- Показывает worst-case для 1% пользователей

**Пример:**
\`\`\`
p50 = 100ms  → половина пользователей ждёт 100ms
p95 = 5000ms → 5% ждут 5 секунд — ПРОБЛЕМА!
p99 = 5000ms
\`\`\`

**Вывод:** p95/p99 показывают реальные проблемы, среднее их скрывает!

### Примеры требований

\`\`\`
NFR-1: p95 response time для API < 200ms
NFR-2: p99 response time для поиска < 500ms
NFR-3: p50 page load time < 1 секунда
\`\`\`

---

## Latency vs Response Time — В чём разница?

### Latency (Задержка)

> "Latency — время **передачи данных по сети**"

**Аналогия:**
- Задержка перед началом готовки пиццы (пока курьер едет к пиццерии)
- Зависит от расстояния между клиентом и сервером

**Примеры:**
\`\`\`
Москва → США: ~150-200ms
Москва → Европа: ~30-50ms
Внутри датацентра: <1ms
3G/4G mobile: 50-300ms
Satellite: 500-800ms
\`\`\`

### Response Time (Время отклика)

> "Response Time = Latency + Processing Time"

**Аналогия:**
- Полное время доставки пиццы = путь туда + готовка + путь обратно

**Пример:**
\`\`\`
Network Latency: 50ms (туда) + 50ms (обратно) = 100ms
Server Processing: 100ms (обработка запроса)
→ Total Response Time = 200ms
\`\`\`

### Как уменьшить Latency?

✅ **CDN (Content Delivery Network)**

Размещение контента ближе к пользователям:

\`\`\`
Без CDN:
Пользователь (Москва) → Сервер (Калифорния) = 200ms latency

С CDN:
Пользователь (Москва) → CDN Edge (Москва) = 5ms latency
\`\`\`

**Аналогия:**
- Без CDN = заказать пиццу из другого города
- С CDN = заказать пиццу из ближайшей пиццерии

---

## Throughput — Пропускная способность

### Что это?

> "Throughput — количество операций, которые система обрабатывает за единицу времени"

**Метрики:**
- **RPS (Requests Per Second)** — запросов в секунду
- **TPS (Transactions Per Second)** — транзакций в секунду
- **QPS (Queries Per Second)** — запросов к БД в секунду

**Аналогия:**
- Сколько пицц пиццерия готовит за час (100 пицц/час = хороший throughput)

### Примеры требований

\`\`\`
NFR-4: API должен поддерживать 10,000 RPS при p95 latency < 200ms
NFR-5: База данных должна обрабатывать 50,000 QPS
\`\`\`

### Throughput vs Latency — Важная разница!

**Высокий throughput НЕ гарантирует низкую latency!**

**Пример:**
\`\`\`
Система A: 100,000 RPS, p95 latency = 2 секунды
→ Обрабатывает много запросов, но пользователи ждут долго ❌

Система B: 10,000 RPS, p95 latency = 100ms
→ Меньше запросов, но пользователи довольны ✅
\`\`\`

**Аналогия:**
- Система A = пиццерия готовит 1000 пицц/час, но каждая доставляется за 2 часа
- Система B = пиццерия готовит 100 пицц/час, но доставка за 15 минут

**Вывод:** Для user-facing API **latency важнее throughput**!

---

## Performance Testing — Тестирование производительности

### 1. Load Testing — Проверка под ожидаемой нагрузкой

**Цель:**
Проверить, что система справляется с обычной нагрузкой

**Пример:**
\`\`\`
Нагрузка: 10,000 одновременных пользователей
Длительность: 1 час
Проверка: p95 latency < 200ms, error rate < 1%
\`\`\`

**Аналогия:**
- Пиццерия в обычный день (50 заказов в час)
- Проверяем: справляется ли она?

### 2. Stress Testing — Проверка до отказа

**Цель:**
Найти breaking point (точку отказа системы)

**Пример:**
\`\`\`
Постепенное увеличение нагрузки:
1,000 → 10,000 → 50,000 → 100,000 пользователей
До момента когда система начнёт падать
\`\`\`

**Аналогия:**
- Сколько максимум заказов пиццерия выдержит, прежде чем кухня сломается?

### 3. Spike Testing — Резкий скачок нагрузки

**Цель:**
Проверить поведение при внезапном росте трафика

**Пример:**
\`\`\`
Baseline: 1,000 пользователей
Резкий скачок: до 50,000 за 1 минуту
Проверка: система остаётся стабильной
\`\`\`

**Аналогия:**
- Black Friday в пиццерии (внезапно 500 заказов вместо обычных 50)
- Справится ли?

### 4. Soak Testing — Длительная нагрузка

**Цель:**
Найти memory leaks и деградацию со временем

**Пример:**
\`\`\`
Нагрузка: 5,000 пользователей
Длительность: 24-48 часов
Проверка: нет утечек памяти
\`\`\`

**Аналогия:**
- Пиццерия работает целый день без перерыва
- Проверяем: не "устанет" ли кухня?

---

## Performance Budget — Бюджет производительности

### Что это?

> "Performance Budget — максимально допустимые значения метрик производительности"

**Пример для веб-страницы:**
\`\`\`
Performance Budget:
- Total page size: < 1 MB
- JavaScript bundle: < 300 KB
- Page load time (4G): < 3 секунды
- LCP (Largest Contentful Paint): < 2.5 секунд
\`\`\`

### Зачем нужен?

**1. Контроль в процессе разработки**
- CI/CD проверяет budget при каждом коммите
- Fail build если превышен

**2. Команда знает ограничения**
- Нельзя бесконечно добавлять фичи без последствий

**Аналогия:**
- Performance Budget = лимит на размер пиццы
- Нельзя добавлять бесконечно топпинги, иначе пицца станет несъедобной

---

## Bottleneck — Узкие места

### Что это?

> "Bottleneck — компонент системы, который **ограничивает** общую производительность"

**Аналогия:**
- Если в пиццерии 10 поваров, но только 1 печь → печь = bottleneck
- Все ждут свою очередь на печь

### Типичные Bottlenecks

**1. Database Bottleneck**
Медленные запросы к БД

**Решения:**
- Индексы
- Query optimization
- Кеширование (Redis)

**2. Network Bottleneck**
Медленная сеть, высокая latency

**Решения:**
- CDN для статики
- Compression (gzip, brotli)

**3. CPU Bottleneck**
Высокая нагрузка на процессор

**Решения:**
- Асинхронная обработка
- Horizontal scaling

---

## Практические сценарии

### Сценарий 1: Медленный API — p95 = 2 секунды

**Ситуация:** API имеет p50 = 50ms (отлично), но p95 = 2 секунды (ужасно). 95% пользователей довольны, но 5% страдают.

**Диагностика через APM (Application Performance Monitoring):**
1. New Relic показал: 95-й перцентиль — медленные database queries
2. Profiling: query \`SELECT * FROM orders WHERE user_id = ?\` **без индекса**
3. Explain plan показал: Full Table Scan (100,000 строк)

**Решение:**
\`\`\`sql
CREATE INDEX idx_user_id ON orders(user_id);
\`\`\`

**Результат:** p95 снизился с 2000ms до 150ms (13x faster!)

**Объяснение:**
Среднее (p50 = 50ms) скрывало проблему 5% запросов. p95/p99 показывают worst-case сценарии. Индекс ускорил медленные запросы.

> 💡 **Важно помнить**: Always measure before optimizing. APM показал bottleneck, не догадки. "Premature optimization is the root of all evil" — сначала profiling, потом оптимизация.

---

### Сценарий 2: Throughput vs Latency — что важнее?

**Ситуация:** Выбор между двумя системами:
- Система A: 100,000 RPS, p95 latency = 5 секунд
- Система B: 10,000 RPS, p95 latency = 100ms

Какую выбрать?

**Анализ для web API:**
- Система A: пользователи ждут 5 секунд → плохой UX, высокий bounce rate ❌
- Система B: пользователи ждут 100ms → хороший UX ✅
- **Выбор:** Система B для user-facing приложений

**Когда Система A лучше:**
- Batch processing (ETL pipelines overnight)
- Analytics (не interactive)
- Background jobs (email sending, report generation)

**Объяснение:**
Высокий throughput бесполезен, если latency убивает UX. Для interactive applications **latency критичнее throughput**.

**Аналогия:**
- Система A = пиццерия готовит 1000 пицц/час, но доставка 5 часов
- Система B = пиццерия готовит 100 пицц/час, но доставка 15 минут

> 💡 **Важно помнить**: Optimize for metrics that matter to users. Web API → optimize latency. Batch processing → optimize throughput.

---

### Сценарий 3: Load Testing перед Black Friday

**Ситуация:** E-commerce сайт ожидает 10x рост трафика (5,000 → 50,000 RPS) в Black Friday. Нужно проверить готовность.

**Load Test план:**

**День -7 (Baseline):**
\`\`\`
Нагрузка: 5,000 RPS (текущая)
Результат: p95 = 100ms, error rate = 0.1%
\`\`\`

**День -3 (Target Load Test):**
\`\`\`
Нагрузка: 50,000 RPS на 1 час
Результат: p95 = 3000ms, error rate = 15%
Bottleneck: Database (CPU 100%, slow queries)
\`\`\`

**Оптимизация:**
- Добавить 2 read replicas для database
- Redis cache для каталога товаров (TTL 5 минут)
- Connection pooling (увеличить pool size с 10 до 50)

**День -1 (Retest):**
\`\`\`
Нагрузка: 50,000 RPS
Результат: p95 = 200ms, error rate = 0.5%
✅ Готово к Black Friday!
\`\`\`

**Объяснение:**
Load Testing выявил bottleneck ДО production. Без теста Black Friday = downtime и потерянная выручка.

**Аналогия:**
- Пиццерия тестирует кухню перед праздниками
- Находит что печь не справляется с нагрузкой
- Добавляет вторую печь ДО праздника

> 💡 **Важно помнить**: Test in production-like environment. Localhost testing не покажет real bottlenecks.

---

### Сценарий 4: Performance Budget для landing page

**Ситуация:** Marketing добавляет фичи на landing page. Через 3 месяца page load time вырос с 2s до 8s. Conversion упал на 40%.

**Проблема:** Нет Performance Budget → бесконтрольный рост.

**Решение — установить Performance Budget:**
\`\`\`
Performance Budget для Landing Page:
- Total page size: < 1.5 MB
- JavaScript bundle: < 400 KB
- Images: < 800 KB
- Page Load Time (4G): < 3 секунды
- LCP (Largest Contentful Paint): < 2.5 секунды
\`\`\`

**CI/CD Integration:**
\`\`\`bash
# Lighthouse CI в pipeline
lighthouse --budget-path=budget.json
# Fail build если budget превышен
\`\`\`

**Результат:** Каждый PR проверяется на budget. Marketing не может добавить 2MB видео без оптимизации.

**Объяснение:**
Performance Budget = guard rails. Без budget команда добавляет фичи → performance деградирует → conversion падает.

**Аналогия:**
- Performance Budget = лимит на калории в пицце
- Без лимита добавляем бесконечно сыра → пицца становится жирной и невкусной

> 💡 **Важно помнить**: "You can't improve what you don't measure". Performance Budget + automated checks в CI/CD = защита от performance regression.

---

### Сценарий 5: CDN снижает latency для global users

**Ситуация:** SaaS приложение. Users в США: latency 50ms. Users в Европе: latency 200ms. Users в Азии: latency 400ms.

**Проблема:** Single origin server в США → high latency для европейских/азиатских пользователей.

**Решение через CDN:**

**Без CDN:**
\`\`\`
User (Лондон) → Origin (Калифорния) = 150ms network latency
User (Токио) → Origin (Калифорния) = 200ms network latency
\`\`\`

**С CDN (Cloudflare/CloudFront):**
\`\`\`
User (Лондон) → CDN Edge (Лондон) = 5ms network latency
User (Токио) → CDN Edge (Токио) = 10ms network latency
\`\`\`

**Результат:**
- Европа: latency снизилась с 200ms до 30ms
- Азия: latency снизилась с 400ms до 50ms
- Bounce rate снизился на 25%

**Объяснение:**
CDN размещает контент ближе к пользователям. Network latency = speed of light limit, нельзя обойти физику. CDN обходит через geographic distribution.

**Аналогия:**
- Без CDN = одна пиццерия в Москве доставляет по всей России
- С CDN = пиццерии в каждом городе

> 💡 **Важно помнить**: CDN для статики — low-hanging fruit. Просто, дёшево, massive impact на global latency.

---

### Сценарий 6: Code Splitting для ускорения загрузки

**Ситуация:** React SPA. Initial bundle.js = 2.5 MB. Page Load Time = 8 секунд на 3G. Users bounce before page loads.

**Проблема:** Монолитный bundle — загружаем весь код сразу, даже для routes которые пользователь не посетит.

**Решение через Code Splitting:**

**До:**
\`\`\`javascript
// bundle.js (2.5 MB)
import HomePage from './HomePage';
import Dashboard from './Dashboard';
import AdminPanel from './AdminPanel'; // 1.5 MB (charts library)
\`\`\`

**После (React.lazy + Suspense):**
\`\`\`javascript
// main.js (500 KB)
const HomePage = React.lazy(() => import('./HomePage'));
const AdminPanel = React.lazy(() => import('./AdminPanel')); // Load on demand
\`\`\`

**Результат:**
- Initial bundle: 2.5 MB → 500 KB (5x reduction!)
- Page Load Time: 8s → 2s
- AdminPanel загружается только если пользователь заходит на /admin

**Объяснение:**
Code Splitting = загружать только нужный код для current route. AdminPanel нужен только admin пользователям (5%), не нужно грузить для всех.

**Аналогия:**
- Без Code Splitting = доставить все пиццы из меню каждому клиенту
- С Code Splitting = доставить только заказанную пиццу

> 💡 **Важно помнить**: Code splitting — не только для routes. Можно split по features: "Load payment module только когда пользователь идёт к checkout".

---

### Сценарий 7: Database N+1 Query Problem

**Ситуация:** API endpoint \`/api/users\` возвращает список пользователей с их постами. 100 пользователей → 10 секунд response time.

**Проблема (N+1):**
\`\`\`javascript
// 1 query
const users = await db.query('SELECT * FROM users LIMIT 100');

// N queries (100 queries!)
for (const user of users) {
  user.posts = await db.query('SELECT * FROM posts WHERE user_id = ?', user.id);
}
// Total: 1 + 100 = 101 queries!
\`\`\`

**Результат:** 101 queries → 100ms per query → 10 секунд total

**Решение (Eager Loading с JOIN):**
\`\`\`sql
SELECT users.*, posts.*
FROM users
LEFT JOIN posts ON posts.user_id = users.id
WHERE users.id IN (SELECT id FROM users LIMIT 100);
-- 1 query вместо 101!
\`\`\`

**Результат:** 1 query → 150ms total (60x faster!)

**Объяснение:**
N+1 problem — классический performance killer. 1 query для главной сущности + N queries для related entities.

**Аналогия:**
- N+1 = курьер доставляет каждую пиццу отдельным рейсом
- JOIN = курьер доставляет все пиццы за один рейс

> 💡 **Важно помнить**: Enable SQL logging в development. Если видите десятки одинаковых queries → N+1 problem.

---

### Сценарий 8: Lazy Loading для изображений

**Ситуация:** E-commerce каталог 100 товаров на странице. Каждый товар = изображение 200 KB. Total images = 20 MB. Page Load Time = 15 секунд на 4G.

**Проблема:** Загружаем все 100 изображений сразу, даже те что below fold (не видны без скролла).

**Решение через Lazy Loading:**

**HTML (native lazy loading):**
\`\`\`html
<img src="product.jpg" loading="lazy" alt="Product">
\`\`\`

**Результат:**
- Initial load: 20 MB → 2 MB (только видимые 10 изображений)
- Page Load Time: 15s → 3s
- Остальные изображения загружаются при скролле

**Объяснение:**
Lazy Loading = load images as needed (при появлении в viewport). Экономит bandwidth и ускоряет initial load.

**Аналогия:**
- Без Lazy Loading = принести все пиццы сразу (даже те что будут съедены завтра)
- С Lazy Loading = приносить пиццы по мере необходимости

> 💡 **Важно помнить**: Lazy loading не только для изображений. Можно lazy load компоненты, videos, iframes.

---

### Сценарий 9: Async Processing для тяжёлых задач

**Ситуация:** User uploads video для обработки. Обработка занимает 2 минуты. User ждёт 2 минуты → timeout, плохой UX.

**Проблема:** Синхронная обработка блокирует response.

**Плохой подход:**
\`\`\`
POST /api/videos
→ Server обрабатывает видео (2 минуты)
→ Response 200 OK
User ждёт 2 минуты (timeout!)
\`\`\`

**Решение через Async Processing:**

**1. Accept and Queue:**
\`\`\`
POST /api/videos
→ Server saves video to storage
→ Push job to queue (RabbitMQ/Kafka)
→ Response 202 Accepted (immediately!)
{
  "id": "video-123",
  "status": "processing",
  "check_url": "/api/videos/video-123/status"
}
\`\`\`

**2. Background Worker processes:**
\`\`\`
Worker picks job from queue
→ Process video (2 минуты)
→ Update status to "completed"
\`\`\`

**3. User polls status:**
\`\`\`
GET /api/videos/video-123/status
{
  "status": "completed",
  "url": "https://cdn.com/video-123.mp4"
}
\`\`\`

**Результат:**
- User response time: 2 минуты → 100ms
- User может делать другие действия пока видео обрабатывается

**Объяснение:**
Async Processing = don't block user response. Тяжёлые задачи в background. User получает быстрый ответ + polling для результата.

**Аналогия:**
- Синхронная = ждать у печи пока пицца готовится
- Асинхронная = заказать, уйти домой, получить звонок когда готово

> 💡 **Важно помнить**: Pattern "Accept, Queue, Process, Notify". User не ждёт — лучший UX.

---

### Сценарий 10: Мониторинг показывает деградацию

**Ситуация:** Production API. Monitoring alert: "p95 latency > 1s for 10 minutes". On-call инженер расследует.

**Мониторинг показывает:**
\`\`\`
12:00 AM: p95 = 150ms (normal)
12:30 AM: p95 = 500ms (degradation)
12:45 AM: p95 = 1200ms (critical)
\`\`\`

**Диагностика через dashboards:**
1. **Application metrics:** Request rate spike (5,000 → 15,000 RPS)
2. **Infrastructure:** Database CPU 100%, Memory 95%
3. **APM traces:** медленные queries к таблице \`orders\`

**Root cause:** Marketing запустил email campaign → 3x traffic spike → database overloaded

**Immediate fix:**
- Enable read replicas routing для read queries
- Increase database connection pool

**Объяснение:**
Monitoring выявил проблему за 10 минут (не за 2 часа от user complaints). Быстрая диагностика → короткий MTTR (Mean Time To Recover).

**Аналогия:**
- Без мониторинга = узнать о сломанной печи от разгневанных клиентов через 2 часа
- С мониторингом = датчик температуры сигналит через 5 минут

> 💡 **Важно помнить**: "You can't fix what you can't see". Monitoring + alerting = early detection.

---

## Типичные ошибки — Чего избегать

### Ошибка 1: Использование среднего вместо перцентилей

❌ **Неправильно:**
Требование: "Average response time < 200ms"

**Проблема:**
\`\`\`
100 запросов:
- 90 запросов: 50ms каждый
- 10 запросов: 2000ms каждый
Average = 245ms (выглядит ОК!)

НО: 10% пользователей ждут 2 секунды!
\`\`\`

✅ **Правильно:**
Требование: "p95 < 200ms, p99 < 500ms"
\`\`\`
p95 = 2000ms → Проблема видна сразу!
\`\`\`

> 💡 **Совет**: Outliers = real users suffering. p95/p99 measure worst-case experience.

---

### Ошибка 2: Оптимизация без измерений (Premature Optimization)

❌ **Неправильно:**
Developer: "Я думаю эта функция медленная. Потрачу неделю на оптимизацию."
После недели: функция ускорилась с 5ms до 2ms.
Но real bottleneck — database query 500ms (не тронут).

**Проблема:** Потрачена неделя на wrong thing.

✅ **Правильно:**
1. **Measure first:** Profiling показал bottleneck — database query 500ms
2. **Optimize bottleneck:** добавить индекс → query 50ms (450ms gain!)
3. **Measure impact:** Total response time 555ms → 105ms (5x faster!)

> 💡 **Совет**: "Premature optimization is the root of all evil" — Donald Knuth. Always profile before optimizing.

---

### Ошибка 3: Нет Performance Budget

❌ **Неправильно:**
\`\`\`
Month 1: bundle.js = 500 KB, page load = 2s
Month 6: bundle.js = 2.5 MB, page load = 8s
Conversion падает на 40%
\`\`\`

**Проблема:** Нет guard rails для performance.

✅ **Правильно:**
\`\`\`
Max bundle size: 800 KB
CI/CD check: if bundle_size > 800KB → fail build
\`\`\`

> 💡 **Совет**: Performance Budget = contract. Automated checks в CI/CD enforce budget.

---

### Ошибка 4: Testing только на localhost/fast network

❌ **Неправильно:**
QA: "Tested on localhost — page loads in 200ms. Ship it!"
Production (users on 3G): page loads in 15 seconds.

**Проблема:** localhost = no network latency, fast disk.

✅ **Правильно:**
- **Network throttling:** Chrome DevTools → 3G/4G simulation
- **Real user monitoring (RUM):** measure actual user metrics

> 💡 **Совет**: Test on slowest expected conditions (3G). If it works on 3G, it flies on 5G.

---

### Ошибка 5: Игнорирование N+1 Query Problem

❌ **Неправильно:**
\`\`\`javascript
for (const user of users) {
  user.posts = await Post.findAll({ where: { userId: user.id } });
}
// 100 users = 101 queries = 5 seconds
\`\`\`

✅ **Правильно:**
\`\`\`javascript
const users = await User.findAll({
  limit: 100,
  include: [Post] // LEFT JOIN posts
});
// 1 query = 150ms
\`\`\`

> 💡 **Совет**: Enable SQL query logging в development. Repeated queries → N+1 problem.

---

### Ошибка 6: Нет мониторинга и алертинга

❌ **Неправильно:**
Нет production monitoring. User: "Site is slow!" → Team узнаёт через 2 часа.
MTTR = 3 hours.

✅ **Правильно:**
\`\`\`
Alerts:
p95 > 500ms for 5 min → Warning
p95 > 1s for 5 min → Critical
MTTR = 15 minutes
\`\`\`

> 💡 **Совет**: "You can't improve what you don't measure". Monitoring = early detection.

---

### Ошибка 7: Frontend загружает всё сразу

❌ **Неправильно:**
Landing page грузит:
- All JavaScript (2 MB)
- All images (10 MB)
Result: 12 MB, 15 seconds load time

✅ **Правильно:**
- **Code splitting:** load JavaScript per route
- **Lazy loading:** load images as they enter viewport
Result: 2 MB initial, 3 seconds load time

> 💡 **Совет**: "Load only what's needed, when it's needed".

---

### Ошибка 8: Нет Capacity Planning

❌ **Неправильно:**
System running at CPU 95%. Marketing launches campaign → CPU 100% → site crashes.

**Проблема:** No headroom для спайков.

✅ **Правильно:**
\`\`\`
Current: CPU 70%
Forecast: In 3 months → CPU 95%
Action: Add servers в месяц 2
\`\`\`

> 💡 **Совет**: Plan capacity 3-6 months ahead. Keep 30% headroom для спайков.

---

## Заключение

**Требования к производительности** — это правила того, как быстро должна работать система.

**Запомните главное:**

1. **Response Time** — используйте перцентили (p95, p99), а не среднее
2. **Throughput** — RPS должны соответствовать нагрузке
3. **Latency** — используйте CDN для global users
4. **Performance Testing** — Load, Stress, Spike testing перед production
5. **Performance Budget** — установите лимиты и контролируйте в CI/CD
6. **Monitoring** — измеряйте метрики, настройте алерты
7. **Optimization** — оптимизируйте на основе данных, а не догадок

**Аналогии для запоминания:**
- Response Time = время доставки пиццы
- Throughput = сколько пицц в час
- Latency = задержка перед началом готовки
- p95 = 95% клиентов получили пиццу быстро, 5% ждали долго

> 🎯 **Золотое правило**: Измеряйте, прежде чем оптимизировать. "Premature optimization is the root of all evil" — Donald Knuth
`;

const questions = [
  {
    question: "Что такое требования к производительности (Performance Requirements)?",
    options: [
      "Требования к функциональности системы",
      "Спецификации того, как быстро система должна выполнять операции при заданной нагрузке",
      "Требования к дизайну интерфейса",
      "Требования к безопасности"
    ],
    correctAnswer: 1,
    explanation: "Требования к производительности (Performance Requirements) — спецификации того, как быстро система должна выполнять операции при заданной нагрузке. Включают: Response Time (время отклика), Throughput (пропускная способность), Resource Utilization (использование ресурсов), Concurrency (одновременность)."
  },
  {
    question: "Что такое Response Time (время отклика)?",
    options: [
      "Время работы сервера",
      "Время от отправки запроса до получения полного ответа",
      "Время обработки на сервере",
      "Время загрузки страницы"
    ],
    correctAnswer: 1,
    explanation: "Response Time (время отклика) — время от отправки запроса до получения полного ответа. Включает: Network Latency (туда) + Server Processing Time (обработка) + Database Query Time (запрос к БД) + Network Latency (обратно). Измеряется в миллисекундах (ms) или секундах."
  },
  {
    question: "Почему p95/p99 перцентили важнее среднего времени отклика?",
    options: [
      "Среднее значение скрывает outliers (выбросы), p95/p99 показывают worst-case для пользователей",
      "p95/p99 проще вычислить",
      "Среднее значение всегда больше p95",
      "Нет разницы, это синонимы"
    ],
    correctAnswer: 0,
    explanation: "Среднее значение скрывает outliers (выбросы), p95/p99 показывают worst-case сценарии. Пример: 90 запросов по 100ms, 10 запросов по 5000ms → Average = 590ms (выглядит ОК), но p95 = 5000ms (5% пользователей ждут 5 секунд!). p95 показывает опыт большинства, p99 — worst-case для небольшой части пользователей."
  },
  {
    question: "Что означает p95 latency < 200ms?",
    options: [
      "95% запросов обрабатываются успешно",
      "95% запросов обрабатываются быстрее 200ms",
      "Среднее время 95ms",
      "95% пользователей довольны"
    ],
    correctAnswer: 1,
    explanation: "p95 latency < 200ms означает, что 95% запросов обрабатываются быстрее 200ms, а 5% запросов медленнее. Это показывает опыт большинства пользователей. p99 < 500ms означало бы, что 99% запросов быстрее 500ms (только 1% медленнее)."
  },
  {
    question: "В чём разница между Latency и Response Time?",
    options: [
      "Нет разницы, это синонимы",
      "Latency — время передачи данных по сети, Response Time — полное время (Latency + Processing)",
      "Latency только для backend, Response Time только для frontend",
      "Latency измеряется в секундах, Response Time в миллисекундах"
    ],
    correctAnswer: 1,
    explanation: "Latency (задержка) — время передачи данных по сети (Client → Server и обратно). Response Time (время отклика) — полное время = Network Latency + Server Processing Time + Database Query Time. Пример: Network Latency 50ms, Processing 100ms → Response Time = 150ms."
  },
  {
    question: "Что такое Throughput (пропускная способность)?",
    options: [
      "Скорость передачи данных",
      "Количество операций, транзакций или запросов, которые система может обработать за единицу времени",
      "Размер данных, передаваемых по сети",
      "Количество серверов в системе"
    ],
    correctAnswer: 1,
    explanation: "Throughput (пропускная способность) — количество операций, транзакций или запросов, которые система может обработать за единицу времени. Метрики: RPS (Requests Per Second), TPS (Transactions Per Second), QPS (Queries Per Second). Пример: API должен поддерживать 10,000 RPS при p95 latency < 200ms."
  },
  {
    question: "Гарантирует ли высокий throughput низкую latency?",
    options: [
      "Да, всегда",
      "Нет, высокий throughput не гарантирует низкую latency",
      "Только если используется CDN",
      "Только при горизонтальном масштабировании"
    ],
    correctAnswer: 1,
    explanation: "Нет, высокий throughput не гарантирует низкую latency! Пример: Система A: 100,000 RPS, p95 latency = 2 секунды (плохо для пользователей). Система B: 10,000 RPS, p95 latency = 100ms (хорошо для пользователей). Система B лучше, хотя throughput ниже. Нужен баланс между throughput и latency."
  },
  {
    question: "Что такое Concurrency (одновременность)?",
    options: [
      "Скорость обработки запросов",
      "Количество одновременных пользователей или запросов, которые система может обрабатывать",
      "Количество серверов",
      "Время ответа системы"
    ],
    correctAnswer: 1,
    explanation: "Concurrency (одновременность) — количество одновременных пользователей или запросов, которые система может обрабатывать. Типы: Concurrent Users (пользователи), Concurrent Requests (HTTP запросы), Concurrent Connections (WebSocket соединения). Формула Little's Law: Concurrency = Throughput × Response Time."
  },
  {
    question: "Что показывает формула Little's Law: Concurrency = Throughput × Response Time?",
    options: [
      "Зависимость между пропускной способностью, временем отклика и одновременностью",
      "Формулу расчёта стоимости",
      "Зависимость между CPU и памятью",
      "Формулу расчёта availability"
    ],
    correctAnswer: 0,
    explanation: "Формула Little's Law показывает зависимость между Throughput, Response Time и Concurrency. Пример: Throughput = 1000 RPS, Response Time = 0.1 секунды → Concurrency = 1000 × 0.1 = 100 одновременных запросов. Используется для capacity planning и определения нужного количества ресурсов."
  },
  {
    question: "Почему важно оставлять headroom (запас) по CPU usage (например, < 70%)?",
    options: [
      "Для экономии электроэнергии",
      "Чтобы оставить запас для спайков нагрузки и предотвратить деградацию производительности",
      "Для красивых графиков в мониторинге",
      "Чтобы сервер работал тише"
    ],
    correctAnswer: 1,
    explanation: "Headroom (запас) по CPU важен для: 1) Спайков нагрузки (внезапный рост трафика), 2) Предотвращения деградации производительности (при 95% CPU система тормозит), 3) Запаса для роста. Правило 70% CPU usage: оставляет 30% запаса. При CPU 95% → система тормозит, latency растёт. При CPU 60-70% → есть запас для роста."
  },
  {
    question: "Что такое Load Testing?",
    options: [
      "Тестирование функциональности",
      "Проверка системы под ожидаемой нагрузкой для измерения response time, throughput и поиска bottlenecks",
      "Тестирование безопасности",
      "Тестирование UI"
    ],
    correctAnswer: 1,
    explanation: "Load Testing — проверка системы под ожидаемой нагрузкой. Цели: проверить что система справляется с целевой нагрузкой, измерить response time (p95, p99), throughput (RPS), найти bottlenecks. Пример: 10,000 одновременных пользователей, 1 час, проверка p95 latency < 200ms, error rate < 1%."
  },
  {
    question: "В чём отличие Stress Testing от Load Testing?",
    options: [
      "Нет отличий",
      "Stress Testing — проверка под экстремальной нагрузкой до отказа для поиска breaking point",
      "Stress Testing только для БД",
      "Load Testing быстрее Stress Testing"
    ],
    correctAnswer: 1,
    explanation: "Load Testing — проверка под ожидаемой нагрузкой (например, 10,000 пользователей). Stress Testing — проверка под экстремальной нагрузкой до отказа. Цели Stress Testing: найти breaking point (точку отказа), проверить graceful degradation, понять лимиты системы. Пример: постепенное увеличение 1,000 → 10,000 → 50,000 → 100,000 пользователей до момента отказа."
  },
  {
    question: "Что такое Spike Testing?",
    options: [
      "Тестирование безопасности",
      "Проверка системы при резком скачке нагрузки для проверки поведения и auto-scaling",
      "Тестирование производительности БД",
      "Медленное увеличение нагрузки"
    ],
    correctAnswer: 1,
    explanation: "Spike Testing — проверка системы при резком скачке нагрузки. Цели: проверить поведение при внезапном росте трафика (например, маркетинговая кампания, вирусный пост), проверить auto-scaling. Пример: Baseline 1,000 пользователей → резкий скачок до 50,000 пользователей за 1 минуту → проверка что система остаётся стабильной."
  },
  {
    question: "Что такое Soak Testing (Endurance Testing)?",
    options: [
      "Быстрое тестирование",
      "Проверка системы под нагрузкой длительное время (24-48 часов) для поиска memory leaks",
      "Тестирование при пиковой нагрузке",
      "Тестирование безопасности"
    ],
    correctAnswer: 1,
    explanation: "Soak Testing (Endurance Testing) — проверка системы под нагрузкой длительное время (24-48 часов). Цели: найти memory leaks, проверить стабильность на длительном периоде, найти деградацию производительности со временем. Пример: 5,000 пользователей, 48 часов, проверка нет memory leaks и деградации."
  },
  {
    question: "Что такое First Contentful Paint (FCP)?",
    options: [
      "Полное время загрузки страницы",
      "Время когда пользователь видит первый контент на странице",
      "Время интерактивности страницы",
      "Размер страницы"
    ],
    correctAnswer: 1,
    explanation: "First Contentful Paint (FCP) — время когда пользователь видит первый контент на странице (текст, изображение, SVG). Метрика важна для perceived performance (воспринимаемая производительность). Требование: FCP < 1 секунда. Отличие от FP (First Paint): FP — первый пиксель, FCP — первый значимый контент."
  },
  {
    question: "Что такое Time to Interactive (TTI)?",
    options: [
      "Время первого клика",
      "Время когда страница полностью интерактивна и может отвечать на действия пользователя",
      "Время загрузки JavaScript",
      "Время загрузки CSS"
    ],
    correctAnswer: 1,
    explanation: "Time to Interactive (TTI) — время когда страница полностью интерактивна и может быстро отвечать на действия пользователя. Требование: TTI < 3.8 секунд. Страница может быть видимой (FCP), но ещё не интерактивной (JavaScript загружается). TTI показывает когда пользователь может начать взаимодействовать."
  },
  {
    question: "Что такое Performance Budget?",
    options: [
      "Бюджет на сервера",
      "Максимально допустимые значения метрик производительности (размер страницы, время загрузки)",
      "Стоимость разработки",
      "Бюджет на тестирование"
    ],
    correctAnswer: 1,
    explanation: "Performance Budget — максимально допустимые значения метрик производительности. Пример: Total page size < 1 MB, JavaScript < 300 KB, Page load time (4G) < 3 секунды, TTI < 3.8 секунд, LCP < 2.5 секунд. Зачем: контроль производительности в CI/CD (fail build если превышен), приоритизация оптимизаций, команда знает ограничения."
  },
  {
    question: "Что такое Bottleneck (узкое место)?",
    options: [
      "Самый быстрый компонент системы",
      "Компонент системы, ограничивающий общую производительность",
      "Резервный сервер",
      "Кеш"
    ],
    correctAnswer: 1,
    explanation: "Bottleneck (узкое место) — компонент системы, ограничивающий общую производительность. Типичные bottlenecks: Database (медленные запросы), Network (высокая latency), CPU (высокая нагрузка), Memory (нехватка RAM). Как найти: Profiling, мониторинг метрик (CPU, Memory, Disk I/O, Network), APM инструменты (New Relic, Datadog)."
  },
  {
    question: "Как CDN (Content Delivery Network) улучшает производительность?",
    options: [
      "Ускоряет обработку на сервере",
      "Размещает контент ближе к пользователям, снижая network latency",
      "Увеличивает CPU мощность",
      "Шифрует данные"
    ],
    correctAnswer: 1,
    explanation: "CDN (Content Delivery Network) размещает контент (статика, изображения, видео) ближе к пользователям через Edge серверы. Пример: Без CDN: Пользователь (Москва) → Сервер (Калифорния) = 200ms latency. С CDN: Пользователь (Москва) → CDN Edge (Москва) = 5ms latency. Снижает latency, ускоряет загрузку страниц."
  },
  {
    question: "Что такое Code Splitting?",
    options: [
      "Разделение команды разработчиков",
      "Разделение JavaScript кода на части, загружаемые по требованию (только нужный код для каждого route)",
      "Разделение базы данных",
      "Разделение CSS файлов"
    ],
    correctAnswer: 1,
    explanation: "Code Splitting — разделение JavaScript кода на части (bundles), загружаемые по требованию. Вместо загрузки всего кода сразу, загружаем только нужный для текущего route. Пример: Route /home → загружаем home.bundle.js, Route /profile → загружаем profile.bundle.js. Уменьшает initial bundle size, ускоряет загрузку."
  },
  {
    question: "Зачем нужен Lazy Loading для изображений?",
    options: [
      "Для шифрования изображений",
      "Отложенная загрузка изображений ниже fold (видимой области) при скролле для ускорения initial load",
      "Для увеличения размера изображений",
      "Для изменения формата изображений"
    ],
    correctAnswer: 1,
    explanation: "Lazy Loading — отложенная загрузка изображений ниже fold (видимой области экрана) при скролле. Преимущества: ускоряет initial page load (загружаем только видимые изображения), экономит трафик (не загружаем изображения, которые пользователь не увидит), улучшает Page Load Time и LCP метрики."
  },
  {
    question: "Что такое Database Query Optimization?",
    options: [
      "Удаление базы данных",
      "Оптимизация SQL запросов через индексы, query optimization, избежание N+1 проблемы",
      "Увеличение размера БД",
      "Создание резервной копии"
    ],
    correctAnswer: 1,
    explanation: "Database Query Optimization — оптимизация SQL запросов для ускорения. Техники: 1) Индексы (CREATE INDEX на часто используемые столбцы), 2) Query optimization (избежать SELECT *, использовать JOIN вместо вложенных запросов), 3) Избежание N+1 проблемы (eager loading), 4) Connection pooling. Цель: p99 query time < 50ms."
  },
  {
    question: "Зачем нужен Asynchronous Processing (асинхронная обработка)?",
    options: [
      "Для замедления системы",
      "Чтобы тяжёлые задачи обрабатывались в фоне через Message Queue, давая быстрый ответ пользователю",
      "Для синхронизации данных",
      "Для шифрования"
    ],
    correctAnswer: 1,
    explanation: "Asynchronous Processing — обработка тяжёлых задач в фоне через Message Queue (Kafka, RabbitMQ) для быстрого ответа пользователю. Пример: User uploads file → Быстрый ответ 202 Accepted → Background processing (конвертация видео, генерация отчёта). Преимущества: низкий response time для пользователя, лучше utilization ресурсов, защита от перегрузок."
  },
  {
    question: "Почему важно мониторить перцентили (p95, p99), а не только среднее?",
    options: [
      "Перцентили проще считать",
      "Перцентили показывают worst-case опыт пользователей, среднее скрывает проблемы",
      "Среднее всегда ошибочно",
      "Перцентили занимают меньше памяти"
    ],
    correctAnswer: 1,
    explanation: "Перцентили (p95, p99) показывают worst-case опыт пользователей, среднее скрывает outliers. Пример мониторинга: Alert если p95 latency > 500ms for 5 minutes → расследовать. Среднее может быть 100ms (выглядит ОК), но p95 = 5 секунд (5% пользователей страдают). Мониторим p95, p99 для реального опыта пользователей."
  },
  {
    question: "Что означает цитата 'Premature optimization is the root of all evil' — Donald Knuth?",
    options: [
      "Оптимизация всегда плохая",
      "Не нужно оптимизировать вслепую без измерений — сначала измерить, найти bottleneck, потом оптимизировать",
      "Оптимизация должна быть первым шагом",
      "Оптимизация бесполезна"
    ],
    correctAnswer: 1,
    explanation: "'Premature optimization is the root of all evil' (Преждевременная оптимизация — корень всех зол) — не оптимизируйте вслепую без измерений. Правильный подход: 1) Измерить performance (profiling), 2) Найти bottleneck (что медленно), 3) Оптимизировать bottleneck, 4) Измерить снова (проверить результат). Оптимизация на основе данных, а не догадок."
  }
];

async function createTest() {
  try {
    console.log('🚀 Создание вопросов для теста "Требования к производительности (Performance Requirements)" (v2)...\n');

    const test = await prisma.test.findUnique({
      where: { id: 'cmkbleoj60009aovna9153e7l' }
    });

    if (!test) {
      throw new Error('❌ Тест не найден!');
    }

    console.log('✅ Найден тест:', test.title);
    console.log(`📊 Текущее количество вопросов: ${await prisma.testQuestion.count({ where: { testId: test.id } })}\n`);

    console.log('📝 Создание лекции v2...');
    const lecture = await prisma.lecture.create({
      data: {
        title: 'Требования к производительности (Performance Requirements) — v2',
        topic: 'performance-requirements',
        content: lectureContent
      }
    });

    console.log(`✅ Лекция v2 создана: ${lecture.id}\n`);

    console.log('❓ Добавление вопросов...\n');

    let addedCount = 0;

    for (let i = 0; i < questions.length; i++) {
      const q = questions[i];

      const existing = await prisma.question.findFirst({
        where: { question: q.question }
      });

      if (existing) {
        console.log(`⚠️  [${i + 1}/${questions.length}] Вопрос уже существует, пропускаем`);
        continue;
      }

      const newQuestion = await prisma.question.create({
        data: {
          question: q.question,
          options: q.options,
          correctAnswer: q.correctAnswer,
          explanation: q.explanation,
          lectureId: lecture.id
        }
      });

      await prisma.testQuestion.create({
        data: {
          testId: test.id,
          questionId: newQuestion.id,
          order: i + 1
        }
      });

      console.log(`✅ [${i + 1}/${questions.length}] ${q.question.substring(0, 70)}...`);
      addedCount++;
    }

    console.log(`\n🎉 Готово!`);
    console.log(`✅ Добавлено вопросов: ${addedCount}`);
    console.log(`📚 Лекция v2 ID: ${lecture.id}`);
    console.log(`📝 Тест ID: ${test.id}`);

    const finalCount = await prisma.testQuestion.count({ where: { testId: test.id } });
    console.log(`📊 Итого вопросов в тесте: ${finalCount}`);

  } catch (error) {
    console.error('❌ Ошибка:', error);
    throw error;
  } finally {
    await prisma.$disconnect();
    await pool.end();
  }
}

createTest();
