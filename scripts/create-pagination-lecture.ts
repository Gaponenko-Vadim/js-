import { PrismaClient } from '@prisma/client';
import { PrismaPg } from '@prisma/adapter-pg';
import { Pool } from 'pg';
import dotenv from 'dotenv';

dotenv.config();

const pool = new Pool({ connectionString: process.env.DATABASE_URL });
const adapter = new PrismaPg(pool);
const prisma = new PrismaClient({ adapter });

function cleanContent(content: string): string {
  return content
    .replace(/\0/g, '')
    .replace(/[\x00-\x08\x0B\x0C\x0E-\x1F]/g, '');
}

const lectureContentRaw = `
# Pagination в REST API

## Введение

Представьте огромную библиотеку с миллионами книг. Вместо того чтобы выгружать все книги сразу (что физически невозможно), библиотекарь дает вам каталог по 20 книг на страницу. Вы можете запросить следующие 20, предыдущие 20 или перейти на конкретную страницу. Именно так работает пагинация в REST API - разбиение больших наборов данных на управляемые порции.

## Зачем нужна пагинация?

### Проблемы без пагинации

1. **Performance degradation**: Возврат 100,000 записей в одном ответе:
   - Создает огромную нагрузку на БД
   - Занимает много памяти на сервере
   - Медленная передача по сети
   - Замораживает клиентское приложение при обработке

2. **Timeout риски**: Большие запросы могут превышать лимиты времени выполнения

3. **UX проблемы**: Пользователь редко нуждается во всех данных сразу

### Преимущества пагинации

✅ **Контролируемая нагрузка** - сервер обрабатывает фиксированное количество записей
✅ **Быстрый первый ответ** - пользователь видит данные мгновенно
✅ **Масштабируемость** - работает с датасетами любого размера
✅ **Предсказуемое использование ресурсов** - легко планировать capacity

---

## Основные подходы к пагинации

### 1. Offset-based Pagination (страничная)

**Концепция**: Пропустить N записей, вернуть M следующих.

#### Query параметры

**Вариант 1: limit/offset**
\`\`\`
GET /api/users?limit=20&offset=40
\`\`\`

- \`limit\` (или \`per_page\`, \`page_size\`) - количество записей на странице
- \`offset\` - сколько записей пропустить (начиная с 0)

**Вариант 2: page/size**
\`\`\`
GET /api/users?page=3&size=20
\`\`\`

- \`page\` - номер страницы (обычно начинается с 1)
- \`size\` (или \`per_page\`) - количество записей на странице

**Связь между подходами:**
\`\`\`
offset = (page - 1) × size
\`\`\`

Страница 3 с размером 20 = offset 40.

#### Пример ответа с metadata

\`\`\`json
{
  "data": [
    { "id": 41, "name": "Alice" },
    { "id": 42, "name": "Bob" },
    ...
  ],
  "pagination": {
    "total": 1000,
    "page": 3,
    "per_page": 20,
    "total_pages": 50,
    "has_more": true,
    "next_page": 4,
    "prev_page": 2
  }
}
\`\`\`

**Metadata поля:**

- \`total\` - общее количество записей (требует COUNT запроса)
- \`page\` - текущая страница
- \`per_page\` - размер страницы
- \`total_pages\` - всего страниц (\`total / per_page\`)
- \`has_more\` - есть ли следующая страница
- \`next_page\` / \`prev_page\` - номера соседних страниц

#### SQL запрос

\`\`\`sql
SELECT * FROM users
ORDER BY id
LIMIT 20 OFFSET 40;
\`\`\`

#### Плюсы offset-based

✅ **Простота реализации** - один параметр, легко понять
✅ **Произвольный доступ** - можно перейти на любую страницу
✅ **Знание общего числа страниц** - отображение "Страница 3 из 50"
✅ **Хорошо для небольших датасетов** - работает быстро до ~10K записей

#### Минусы offset-based

❌ **Performance degradation с большими offset**
   - \`OFFSET 100000\` заставляет БД сканировать 100K строк и выбросить их
   - Линейный рост времени выполнения: offset 1M = миллион пропущенных строк

❌ **Duplicates и skipped items при изменении данных**
   Пример:
   1. Пользователь на странице 1 (записи 1-20)
   2. Добавляется новая запись в начало списка
   3. Пользователь переходит на страницу 2
   4. Запись #20 теперь на позиции 21 → показывается дважды

❌ **COUNT(*) дорогой для огромных таблиц**
   Запрос \`SELECT COUNT(*)\` на таблице с 10M записей может занимать секунды.

#### Когда использовать offset-based

- Датасеты до 100K записей
- Нужен произвольный доступ к страницам (UI с номерами страниц)
- Данные редко изменяются
- Приложения с табличным отображением (admin панели)

---

### 2. Cursor-based Pagination (курсорная)

**Концепция**: Использовать уникальный идентификатор последней записи как "курсор" для получения следующих.

#### Query параметры

\`\`\`
GET /api/users?limit=20&cursor=eyJpZCI6NDAsImNyZWF0ZWRfYXQiOiIyMDI0LTAxLTE1In0=
\`\`\`

- \`limit\` - количество записей
- \`cursor\` - закодированный указатель на позицию (обычно base64)

**Cursor encoding:**
Курсор часто содержит несколько полей:
\`\`\`json
{ "id": 40, "created_at": "2024-01-15T10:00:00Z" }
\`\`\`
Кодируется в base64: \`eyJpZCI6NDAsImNyZWF0ZWRfYXQiOiIyMDI0LTAxLTE1VDEwOjAwOjAwWiJ9\`

#### Пример ответа

\`\`\`json
{
  "data": [
    { "id": 41, "name": "Alice" },
    { "id": 42, "name": "Bob" }
  ],
  "pagination": {
    "next_cursor": "eyJpZCI6NjAsImNyZWF0ZWRfYXQi...",
    "has_more": true
  }
}
\`\`\`

**Важно**: \`next_cursor\` - это курсор для получения следующей порции. Если \`has_more: false\`, курсора нет.

#### SQL запрос

\`\`\`sql
SELECT * FROM users
WHERE id > 40
ORDER BY id ASC
LIMIT 20;
\`\`\`

Или с composite cursor (id + created_at):
\`\`\`sql
SELECT * FROM users
WHERE (created_at, id) > ('2024-01-15 10:00:00', 40)
ORDER BY created_at, id
LIMIT 20;
\`\`\`

#### Плюсы cursor-based

✅ **Consistent performance** - всегда быстро, независимо от глубины
✅ **Нет дубликатов/пропусков** - стабильная пагинация при изменении данных
✅ **Масштабируется на миллионы записей** - индексы работают эффективно
✅ **Не требует COUNT(*)** - меньше нагрузки на БД

#### Минусы cursor-based

❌ **Нет произвольного доступа** - можно только "вперед/назад", нельзя перейти на страницу 42
❌ **Сложнее реализация** - нужно кодировать/декодировать курсоры
❌ **Нельзя узнать общее количество** - неизвестно "сколько всего страниц"

#### Когда использовать cursor-based

- Бесконечный скролл (infinite scroll)
- Реал-тайм ленты (соцсети, новости)
- Большие датасеты (миллионы записей)
- Часто изменяющиеся данные

---

### 3. Keyset Pagination (Seek Method)

**Концепция**: Частный случай cursor-based, где курсор - это значение конкретного поля (не закодированное).

\`\`\`
GET /api/posts?limit=20&since_id=12345
GET /api/posts?limit=20&max_id=12345
\`\`\`

- \`since_id\` - получить записи с ID больше указанного (newer than)
- \`max_id\` - получить записи с ID меньше указанного (older than)

#### SQL запрос

\`\`\`sql
-- Новые посты
SELECT * FROM posts
WHERE id > 12345
ORDER BY id ASC
LIMIT 20;

-- Старые посты
SELECT * FROM posts
WHERE id < 12345
ORDER BY id DESC
LIMIT 20;
\`\`\`

#### Преимущества

- Простой URL (не нужен base64)
- Те же плюсы что у cursor-based
- Легко кешируется

**Используется в:** Twitter API, GitHub API, Instagram API.

---

### 4. Bidirectional Pagination (двунаправленная)

**Концепция**: Возможность получать данные как вперед, так и назад от текущей позиции.

#### Cursor параметры

\`\`\`
GET /api/messages?first=20&after=cursor1   # Вперед: 20 записей после cursor1
GET /api/messages?last=20&before=cursor2   # Назад: 20 записей до cursor2
\`\`\`

- \`first\` + \`after\` - получить N записей после курсора
- \`last\` + \`before\` - получить N записей перед курсором

#### Пример ответа

\`\`\`json
{
  "edges": [
    {
      "node": { "id": "msg1", "text": "Hello" },
      "cursor": "Y3Vyc29yMQ=="
    },
    {
      "node": { "id": "msg2", "text": "World" },
      "cursor": "Y3Vyc29yMg=="
    }
  ],
  "pageInfo": {
    "hasNextPage": true,
    "hasPreviousPage": true,
    "startCursor": "Y3Vyc29yMQ==",
    "endCursor": "Y3Vyc29yMg=="
  }
}
\`\`\`

#### GraphQL Cursor Connections

Этот подход стандартизирован в **Relay Cursor Connections Specification**:

- \`edges[]\` - массив записей с курсорами
- \`node\` - сама запись
- \`cursor\` - курсор для каждой записи
- \`pageInfo\` - информация о странице
  - \`hasNextPage\` / \`hasPreviousPage\`
  - \`startCursor\` / \`endCursor\` - курсоры первого/последнего элемента

#### Когда использовать

- Чат приложения (скролл вверх/вниз по истории)
- Календари (листание месяцев вперед/назад)
- Временные линии (timelines)

---

## Link Headers (RFC 5988)

### Концепция

Вместо встроенной metadata в JSON, используйте HTTP заголовок \`Link\` для передачи URL следующей/предыдущей страницы.

#### Пример

\`\`\`http
GET /api/users?page=3&per_page=20

HTTP/1.1 200 OK
Link: </api/users?page=1&per_page=20>; rel="first",
      </api/users?page=2&per_page=20>; rel="prev",
      </api/users?page=4&per_page=20>; rel="next",
      </api/users?page=50&per_page=20>; rel="last"
Content-Type: application/json

[
  { "id": 41, "name": "Alice" },
  { "id": 42, "name": "Bob" }
]
\`\`\`

#### Rel типы

- \`rel="first"\` - первая страница
- \`rel="prev"\` - предыдущая страница
- \`rel="next"\` - следующая страница
- \`rel="last"\` - последняя страница

#### Преимущества

✅ **Чистый response body** - только данные, без служебной информации
✅ **Стандарт RFC 5988** - понятная семантика
✅ **Self-descriptive URLs** - клиент не строит URLs вручную
✅ **Простой парсинг** - много библиотек умеют парсить Link headers

#### Недостатки

❌ **Не видно в JSON** - нужно смотреть headers
❌ **Менее очевидно для новых разработчиков**
❌ **Сложнее тестировать** в браузерных инструментах

**Используется в:** GitHub API, GitLab API.

---

## Сравнительная таблица подходов

| Критерий | Offset-based | Cursor-based | Keyset |
|----------|--------------|--------------|--------|
| **Произвольный доступ** | ✅ Да | ❌ Нет | ❌ Нет |
| **Performance на больших offset** | ❌ Медленно | ✅ Быстро | ✅ Быстро |
| **Стабильность при изменениях** | ❌ Дубликаты/пропуски | ✅ Стабильно | ✅ Стабильно |
| **Знание общего числа** | ✅ Да (с COUNT) | ❌ Нет | ❌ Нет |
| **Простота реализации** | ✅ Простая | ⚠️ Средняя | ✅ Простая |
| **Use case** | Admin панели, таблицы | Бесконечный скролл | Ленты, feeds |

---

## Performance соображения

### 1. Индексы для пагинации

**Offset-based требует индекс на ORDER BY поле:**
\`\`\`sql
CREATE INDEX idx_users_id ON users(id);
\`\`\`

Без индекса \`OFFSET 10000 LIMIT 20\` сканирует 10020 строк **sequentially** (крайне медленно).

**Cursor-based требует индекс на cursor поля:**
\`\`\`sql
CREATE INDEX idx_posts_created_id ON posts(created_at, id);
\`\`\`

Composite index для \`WHERE (created_at, id) > (...)\`.

### 2. COUNT(*) оптимизация

Проблема: \`SELECT COUNT(*) FROM users WHERE active = true\` медленный на 10M+ таблицах.

**Решение 1: Приблизительный count**
\`\`\`sql
SELECT reltuples::bigint FROM pg_class WHERE relname = 'users';
\`\`\`
Дает примерное число строк, обновляется VACUUM.

**Решение 2: Не показывать total**
Вместо "Страница 3 из 50" показать только "Есть ещё данные".

**Решение 3: Кеширование count**
Сохранить результат COUNT(*) в Redis на 5 минут.

### 3. Ограничение максимального offset

Защита от \`?offset=999999999\`:
\`\`\`javascript
const MAX_OFFSET = 10000;
if (offset > MAX_OFFSET) {
  return res.status(400).json({
    error: "Offset too large. Use cursor-based pagination for deep pages."
  });
}
\`\`\`

**Пример:** Elasticsearch по умолчанию лимитирует offset до 10,000.

### 4. Default limits

Всегда устанавливайте дефолтное значение:
\`\`\`javascript
const limit = Math.min(req.query.limit || 20, 100); // Max 100
\`\`\`

Предотвращает \`?limit=999999\` атаки.

---

## Типичные задачи

### Задача 1: Реализовать offset-based pagination

\`\`\`javascript
// GET /api/products?page=2&per_page=10

app.get('/api/products', async (req, res) => {
  const page = parseInt(req.query.page) || 1;
  const perPage = Math.min(parseInt(req.query.per_page) || 20, 100);
  const offset = (page - 1) * perPage;

  const [products, total] = await Promise.all([
    db.products.findMany({
      skip: offset,
      take: perPage,
      orderBy: { id: 'asc' }
    }),
    db.products.count()
  ]);

  const totalPages = Math.ceil(total / perPage);

  res.json({
    data: products,
    pagination: {
      total,
      page,
      per_page: perPage,
      total_pages: totalPages,
      has_more: page < totalPages
    }
  });
});
\`\`\`

### Задача 2: Реализовать cursor-based pagination

\`\`\`javascript
// GET /api/posts?limit=20&cursor=eyJpZCI6MTAwfQ==

app.get('/api/posts', async (req, res) => {
  const limit = Math.min(parseInt(req.query.limit) || 20, 100);
  let cursor = null;

  if (req.query.cursor) {
    const decoded = Buffer.from(req.query.cursor, 'base64').toString();
    cursor = JSON.parse(decoded);
  }

  const posts = await db.posts.findMany({
    where: cursor ? { id: { gt: cursor.id } } : {},
    take: limit + 1, // Берем на 1 больше чтобы понять has_more
    orderBy: { id: 'asc' }
  });

  const hasMore = posts.length > limit;
  const data = hasMore ? posts.slice(0, limit) : posts;

  const nextCursor = hasMore
    ? Buffer.from(JSON.stringify({ id: data[data.length - 1].id })).toString('base64')
    : null;

  res.json({
    data,
    pagination: {
      next_cursor: nextCursor,
      has_more: hasMore
    }
  });
});
\`\`\`

### Задача 3: Link headers

\`\`\`javascript
app.get('/api/users', async (req, res) => {
  const page = parseInt(req.query.page) || 1;
  const perPage = 20;

  const total = await db.users.count();
  const totalPages = Math.ceil(total / perPage);

  const users = await db.users.findMany({
    skip: (page - 1) * perPage,
    take: perPage
  });

  const links = [];
  const baseUrl = \`\${req.protocol}://\${req.get('host')}\${req.path}\`;

  links.push(\`<\${baseUrl}?page=1&per_page=\${perPage}>; rel="first"\`);
  if (page > 1) {
    links.push(\`<\${baseUrl}?page=\${page - 1}&per_page=\${perPage}>; rel="prev"\`);
  }
  if (page < totalPages) {
    links.push(\`<\${baseUrl}?page=\${page + 1}&per_page=\${perPage}>; rel="next"\`);
  }
  links.push(\`<\${baseUrl}?page=\${totalPages}&per_page=\${perPage}>; rel="last"\`);

  res.set('Link', links.join(', '));
  res.json(users);
});
\`\`\`

---

## Best Practices

### 1. Consistency в именовании

**Выберите одну схему:**
- \`limit/offset\` (распространенная)
- \`page/per_page\` (читаемая)
- \`page_size/page_number\` (явная)

**Не смешивайте:**
❌ \`/users?limit=20&page=2\` - непонятно

### 2. Validation параметров

\`\`\`javascript
const page = Math.max(1, parseInt(req.query.page) || 1);
const limit = Math.min(Math.max(1, parseInt(req.query.limit) || 20), 100);
\`\`\`

- Минимум: 1
- Максимум: установите разумный лимит (например 100)
- Дефолт: если не передан

### 3. Всегда возвращайте metadata

Даже если клиент не использует, metadata полезна для debugging:
\`\`\`json
{
  "data": [...],
  "pagination": {
    "page": 1,
    "per_page": 20,
    "total": 500
  }
}
\`\`\`

### 4. Документируйте поведение

- Нумерация страниц начинается с 0 или 1?
- Что возвращается если page > total_pages? (пустой массив или 404?)
- Максимальный limit?
- Формат курсора?

### 5. Используйте hypermedia (HATEOAS)

Включайте готовые URLs:
\`\`\`json
{
  "data": [...],
  "links": {
    "first": "/api/users?page=1",
    "prev": "/api/users?page=1",
    "next": "/api/users?page=3",
    "last": "/api/users?page=50"
  }
}
\`\`\`

Клиент не должен конструировать URLs вручную.

### 6. Cursor encoding

Не раскрывайте внутреннюю структуру:
✅ \`cursor=eyJpZCI6MTAwfQ==\` (base64)
❌ \`cursor={"id":100}\` (plain JSON)

Это позволит изменить формат курсора без breaking changes.

### 7. Сортировка для cursor-based

Cursor требует **стабильной сортировки**:
❌ \`ORDER BY name\` - если два user.name одинаковые, порядок не детерминирован
✅ \`ORDER BY name, id\` - добавьте уникальное поле для tie-breaking

### 8. Тестирование edge cases

- Пустая коллекция (\`total: 0\`)
- Одна запись (\`total: 1\`)
- page > total_pages
- Отрицательные параметры (\`page=-1\`)
- Нецелые числа (\`limit=20.5\`)

---

## Заключение

**Выбор подхода:**

- **Offset-based**: простые интерфейсы, таблицы, admin панели, малые датасеты
- **Cursor-based**: бесконечный скролл, большие датасеты, реал-тайм ленты
- **Keyset**: API для мобильных приложений, когда нужна простота
- **Bidirectional**: чаты, календари, временные линии

**Золотое правило**: начните с offset-based для простоты, переходите на cursor-based когда датасет превышает 100K записей или нужна абсолютная стабильность при изменениях.

Пагинация - это компромисс между UX, performance и сложностью реализации. Выбирайте подход исходя из характера данных и требований продукта.
`;

const lectureContent = cleanContent(lectureContentRaw);

async function createLecture() {
  try {
    console.log('🔧 Создание лекции "Pagination"...\n');

    const lecture = await prisma.lecture.create({
      data: {
        title: 'Pagination в REST API',
        topic: 'Pagination',
        content: lectureContent,
      },
    });

    console.log(`✅ Лекция создана с ID: ${lecture.id}\n`);

    const test = await prisma.test.findFirst({
      where: {
        title: { contains: 'Pagination' },
      },
      include: {
        questions: {
          include: {
            question: true,
          },
        },
      },
    });

    if (!test) {
      console.log('❌ Тест "Pagination" не найден');
      return;
    }

    console.log(`📝 Найден тест: ${test.title}`);
    console.log(`❓ Количество вопросов: ${test.questions.length}\n`);

    const questionIds = test.questions.map((tq) => tq.questionId);

    const updateResult = await prisma.question.updateMany({
      where: {
        id: {
          in: questionIds,
        },
      },
      data: {
        lectureId: lecture.id,
      },
    });

    console.log(`✅ Обновлено вопросов: ${updateResult.count}`);
    console.log(`✅ Все вопросы теста "${test.title}" связаны с лекцией!\n`);

    const verification = await prisma.question.findMany({
      where: {
        id: { in: questionIds },
        lectureId: lecture.id,
      },
    });

    console.log('📊 Проверка покрытия:');
    console.log(`   Всего вопросов в тесте: ${questionIds.length}`);
    console.log(`   Связано с лекцией: ${verification.length}`);
    console.log(
      `   Покрытие: ${((verification.length / questionIds.length) * 100).toFixed(1)}%`
    );
  } catch (error) {
    console.error('❌ Ошибка:', error);
    throw error;
  } finally {
    await prisma.$disconnect();
  }
}

createLecture();
