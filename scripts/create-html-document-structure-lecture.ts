import { PrismaClient } from '@prisma/client';
import { PrismaPg } from '@prisma/adapter-pg';
import { Pool } from 'pg';
import dotenv from 'dotenv';
import path from 'path';

const envPath = path.resolve(__dirname, '..', '.env');
dotenv.config({ path: envPath });

const pool = new Pool({ connectionString: process.env.DATABASE_URL });
const adapter = new PrismaPg(pool);
const prisma = new PrismaClient({ adapter });

const lectureContent = `# HTML: основы и структура документа

## Введение: как чертёж дома 🏠

Представьте дом без чертежа.  
Стены есть, но непонятно, где вход, где кухня и где выход.  
HTML‑документ — это такой же **чертёж страницы**, по которому браузер понимает, что и где показывать.

💡 **Совет:** структура HTML — это порядок и смысл. Без неё даже красивый дизайн ломается.

---

## Что такое HTML

**HTML (HyperText Markup Language)** — язык разметки, который описывает структуру страницы.  
Он отвечает за то, **что** находится на странице и **как это расположено логически**.

✅ **Вывод:** HTML — это каркас. Дизайн (CSS) и логика (JS) — отдельные слои.

---

## Из чего состоит HTML‑документ

Минимальная структура всегда включает:

1) DOCTYPE — сообщает браузеру, что это HTML5  
2) HTML — корневой тег документа  
3) HEAD — метаданные (невидимые для пользователя)  
4) BODY — весь видимый контент

💡 **Совет:** если убрать любой из этих пунктов, браузер будет угадывать — а это всегда ошибки.

---

## Базовый каркас документа

~~~html
<!DOCTYPE html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Название страницы</title>
  </head>
  <body>
    <h1>Главный заголовок</h1>
    <p>Контент страницы</p>
  </body>
</html>
~~~

✅ **Вывод:** этот скелет — база для любых сайтов.

---

## Тег HEAD — «служебная часть»

В HEAD размещают **метаданные**, которые нужны браузеру и поисковикам:

- meta charset — кодировка (UTF‑8)  
- meta viewport — адаптивность  
- title — заголовок вкладки  
- meta description — описание страницы  
- link — подключение CSS  

💡 **Совет:** meta charset ставьте первым — это защищает от проблем с кодировкой.

✅ **Вывод:** всё, что не должно быть видно на странице, — в HEAD.

---

## Тег BODY — «видимая часть»

В BODY находится **всё, что видит пользователь**:

- тексты и заголовки  
- кнопки, формы  
- изображения  
- блоки контента  

💡 **Совет:** один главный заголовок h1 на страницу — это стандарт для структуры и SEO.

✅ **Вывод:** BODY — это сценарий, который читает пользователь.

---

## Атрибут lang — маленькая деталь с большим смыслом

HTML с lang="ru" помогает:
- скринридерам правильно озвучивать текст  
- поисковикам понимать язык страницы  
- браузеру выбирать правила переноса

✅ **Вывод:** lang — обязательный атрибут для доступности.

---

## Где подключать CSS и JS

**CSS** подключают в HEAD, чтобы страница не мигала без стилей.  
**JS** лучше подключать в конце BODY или с defer, чтобы не блокировать загрузку.

💡 **Совет:** если JS критичен, используйте defer, а не «голый» script в HEAD.

✅ **Вывод:** порядок подключения влияет на скорость и стабильность страницы.

---

## Частые ошибки

1) ❌ Нет DOCTYPE → браузер включает quirks mode  
2) ❌ Нет lang → проблемы с доступностью  
3) ❌ Нет title → вкладка без названия  
4) ❌ Скрипты блокируют загрузку страницы  
5) ❌ Метаданные в BODY → нарушение структуры

💡 **Совет:** ошибки структуры — это не «мелочи», они ломают UX и SEO.

---

## Best Practices

- Держите каркас HTML простым и читаемым  
- Всегда указывайте lang  
- Держите HEAD чистым: только метаданные  
- Не смешивайте структуру и оформление  

✅ **Вывод:** чистая структура = быстрый и понятный сайт.

---

## Заключение

HTML‑документ — это фундамент.  
Если фундамент кривой, никакой CSS не спасёт.

### Ключевые моменты

- 🎯 Структура = порядок и смысл  
- 🎯 HEAD для метаданных, BODY для контента  
- 🎯 DOCTYPE, lang, title — обязательны  
- 🎯 Чистый каркас облегчает поддержку
`;

const scenariosContent = `## Популярные сценарии

### Сценарий 1: Нет DOCTYPE

❌ **Неправильно**:  
Документ начинается сразу с HTML.

✅ **Правильно**:  
Всегда указывать DOCTYPE.

**Почему:** без DOCTYPE браузер может перейти в quirks mode.

**Практический пример:**
~~~
DOC-01: Документ начинается с DOCTYPE.
DOC-02: Версия HTML = HTML5.
~~~

### Сценарий 2: Нет lang

❌ **Неправильно**:  
HTML без указания языка.

✅ **Правильно**:  
HTML с lang="ru".

**Почему:** влияет на доступность и SEO.

**Практический пример:**
~~~
DOC-03: Атрибут lang обязателен для всех страниц.
~~~

### Сценарий 3: Charset указан слишком поздно

❌ **Неправильно**:  
meta charset размещён внизу HEAD.

✅ **Правильно**:  
meta charset идёт первым.

**Почему:** иначе возможна неверная кодировка.

**Практический пример:**
~~~
DOC-04: meta charset UTF-8 указывается первой строкой в HEAD.
~~~

### Сценарий 4: Нет title

❌ **Неправильно**:  
title не задан.

✅ **Правильно**:  
Каждая страница имеет уникальный title.

**Почему:** вкладка без названия и хуже SEO.

**Практический пример:**
~~~
DOC-05: Каждая HTML-страница имеет уникальный title.
~~~

### Сценарий 5: CSS подключён в BODY

❌ **Неправильно**:  
CSS подключён после контента.

✅ **Правильно**:  
CSS подключается в HEAD.

**Почему:** иначе страница «мигает» без стилей.

**Практический пример:**
~~~
DOC-06: Все CSS-файлы подключаются в HEAD.
~~~

### Сценарий 6: Скрипты блокируют рендер

❌ **Неправильно**:  
script в HEAD без defer.

✅ **Правильно**:  
script в конце BODY или с defer.

**Почему:** блокировка загрузки контента.

**Практический пример:**
~~~
DOC-07: Скрипты не блокируют рендер: defer или конец BODY.
~~~

### Сценарий 7: Метаданные в BODY

❌ **Неправильно**:  
meta description находится в BODY.

✅ **Правильно**:  
Все метаданные размещаются в HEAD.

**Почему:** нарушение структуры документа.

**Практический пример:**
~~~
DOC-08: Метаданные (meta/title/link) размещаются в HEAD.
~~~
`;

const tasksContent = `## Задания для практики 🧪

### Задание 1: Каркас страницы
> **Ситуация:** нужен минимальный HTML для лендинга "Портфолио".  
> **Что сделать:** составьте каркас с DOCTYPE, html/lang, head и body.  
> **Критерий:** есть title, meta charset и один h1 в body.

**Ответ:**
~~~html
<!DOCTYPE html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <title>Портфолио</title>
  </head>
  <body>
    <h1>Портфолио</h1>
  </body>
</html>
~~~

**Объяснение:** это минимальный валидный каркас HTML5 с базовыми метаданными и главным заголовком.

---

### Задание 2: Исправьте структуру
Ниже фрагмент с ошибками. Исправьте минимум 4 ошибки.

~~~html
<html>
  <body>
    <meta charset="UTF-8" />
    <title>Мой сайт</title>
  </body>
</html>
~~~

> **Подсказка:** DOCTYPE, head и порядок метаданных.

**Ответ:**
~~~html
<!DOCTYPE html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <title>Мой сайт</title>
  </head>
  <body></body>
</html>
~~~

**Объяснение:** метаданные должны быть в head, нужен DOCTYPE и атрибут lang.

---

### Задание 3: Метаданные для мобильных
> **Ситуация:** страница плохо выглядит на телефоне.  
> **Что сделать:** добавьте корректный meta viewport и обновите title.  
> **Критерий:** страница масштабируется правильно, заголовок читаемый.

**Ответ:**
~~~html
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Курс по HTML — базовая структура</title>
~~~

**Объяснение:** viewport задает правильный масштаб, title помогает пользователю и SEO.

---

### Задание 4: Где подключать CSS и JS
> **Ситуация:** страница "мигает" без стилей и долго рисуется.  
> **Что сделать:** правильно подключите CSS и JS, не блокируя рендер.  
> **Критерий:** CSS в head, JS — в конце body или с defer.

**Ответ:**
~~~html
<head>
  <link rel="stylesheet" href="/styles/main.css" />
</head>
<body>
  ...
  <script src="/scripts/app.js" defer></script>
</body>
~~~

**Объяснение:** CSS в head предотвращает FOUC, defer не блокирует загрузку страницы.

---

### Задание 5: Язык документа
> **Ситуация:** скринридер читает текст с неправильным произношением.  
> **Что сделать:** задайте корректный lang и проверьте совместимость.  
> **Критерий:** язык явно указан в теге html.

**Ответ:**
~~~html
<html lang="ru">
~~~

**Объяснение:** lang помогает доступности и правильной обработке текста.

---

### Задание 6: Структура страницы
> **Ситуация:** нужно оформить простую страницу курса.  
> **Что сделать:** добавьте header, main и footer с базовым контентом.  
> **Критерий:** структура читается как логичный каркас страницы.

**Ответ:**
~~~html
<header>
  <h1>Курс по HTML</h1>
</header>
<main>
  <section>
    <h2>О курсе</h2>
    <p>Быстрый старт для новичков.</p>
  </section>
</main>
<footer>
  <p>© 2026 Учебный проект</p>
</footer>
~~~

**Объяснение:** header/main/footer дают читаемую структуру и улучшают навигацию.

---

### Задание 7: Метаданные не в том месте
Найдите метаданные в body и перенесите их в head.

> **Критерий:** в body остается только видимый контент.

**Ответ:**
~~~html
<head>
  <meta charset="UTF-8" />
  <title>Название страницы</title>
</head>
<body>
  <h1>Контент</h1>
</body>
~~~

**Объяснение:** метаданные относятся к head, body должен содержать только контент.

---

### Задание 8: Правильный title
> **Ситуация:** вкладка браузера показывает "Страница".  
> **Что сделать:** придумайте осмысленный title для страницы курса HTML.  
> **Критерий:** title описывает содержание и хорошо выглядит в вкладке.

**Ответ:** Курс по HTML: основы структуры документа

**Объяснение:** title должен быть конкретным и отражать тему страницы.

---

### Задание 9: Проверка DOCTYPE
> **Ситуация:** верстка ломается в старых браузерах.  
> **Что сделать:** добавьте корректный DOCTYPE и проверьте результат.  
> **Критерий:** документ работает в стандартном режиме.

**Ответ:**
~~~html
<!DOCTYPE html>
~~~

**Объяснение:** DOCTYPE переводит браузер в стандартный режим и стабилизирует рендер.

---

### Задание 10: Полный базовый шаблон
> **Ситуация:** нужен универсальный шаблон для новых страниц.  
> **Что сделать:** соберите полный базовый шаблон (head + body + подключение CSS/JS).  
> **Критерий:** шаблон можно переиспользовать без правок структуры.

**Ответ:**
~~~html
<!DOCTYPE html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Новая страница</title>
    <link rel="stylesheet" href="/styles/main.css" />
  </head>
  <body>
    <h1>Заголовок</h1>
    <script src="/scripts/app.js" defer></script>
  </body>
</html>
~~~

**Объяснение:** это повторно используемый каркас с корректными метаданными и подключениями.
`;

const exampleContent = `## Пример: базовый HTML‑документ 📄

Разрабатывается условно такой сайт: лендинг онлайн‑курса для новичков.  
После всех митингов, общения с командой и постановки задачи нужно было  
решить проблему понятной структуры страницы и быстрой загрузки.  
Ниже — полный пример корректного HTML‑каркаса.

---

~~~html
<!DOCTYPE html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Курс по HTML и CSS</title>
    <meta name="description" content="Быстрый старт в HTML и CSS для новичков" />
    <link rel="stylesheet" href="/styles/main.css" />
  </head>
  <body>
    <header>
      <h1>Курс по HTML и CSS</h1>
      <p>Научитесь создавать современные сайты с нуля</p>
    </header>

    <main>
      <section>
        <h2>Для кого этот курс</h2>
        <p>Для начинающих, которым нужен понятный старт</p>
      </section>

      <section>
        <h2>Что вы получите</h2>
        <ul>
          <li>Понимание структуры HTML</li>
          <li>Навыки работы с базовой семантикой</li>
          <li>Уверенность в верстке страниц</li>
        </ul>
      </section>
    </main>

    <footer>
      <p>© 2026 Мамин программист</p>
    </footer>

    <script src="/scripts/app.js" defer></script>
  </body>
</html>
~~~

💡 **Совет:** начните с чистого каркаса, и только потом добавляйте стили и логику.
`;

async function createHtmlDocumentStructureLecture() {
  try {
    console.log('🚀 Создание лекции "HTML: основы и структура документа"...\n');

    const test = await prisma.test.findFirst({
      where: { title: 'HTML: основы и структура документа' },
      include: {
        questions: {
          include: { question: true },
          orderBy: { order: 'asc' }
        }
      }
    });

    if (!test) {
      throw new Error('❌ Тест "HTML: основы и структура документа" не найден!');
    }

    await prisma.lecture.deleteMany({
      where: { title: 'HTML: основы и структура документа' }
    });

    const lecture = await prisma.lecture.create({
      data: {
        title: 'HTML: основы и структура документа',
        topic: 'HTML',
        content: lectureContent,
        scenariosContent,
        exampleContent,
        tasksContent
      }
    });

    let updatedCount = 0;
    for (const tq of test.questions) {
      await prisma.question.update({
        where: { id: tq.questionId },
        data: { lectureId: lecture.id }
      });
      updatedCount++;
    }

    console.log(`✅ Лекция создана с ID: ${lecture.id}`);
    console.log(`✅ Привязано вопросов: ${updatedCount}`);
  } catch (error) {
    console.error('❌ Ошибка:', error);
    throw error;
  } finally {
    await prisma.$disconnect();
    await pool.end();
  }
}

createHtmlDocumentStructureLecture();
