import { PrismaClient } from '@prisma/client';
import { PrismaPg } from '@prisma/adapter-pg';
import { Pool } from 'pg';
import dotenv from 'dotenv';
import path from 'path';

const envPath = path.resolve(__dirname, '..', '.env');
dotenv.config({ path: envPath });

const pool = new Pool({ connectionString: process.env.DATABASE_URL });
const adapter = new PrismaPg(pool);
const prisma = new PrismaClient({ adapter });

const lectureContent = `# Требования к UI/UX и Usability

## Введение: Как дверь может научить UX-дизайну

Представьте себе две двери:

**Дверь A:**
- Видна ручка → понятно, что её нужно тянуть
- Стрелка "Потяните" → подтверждает ожидание
- Ручка на комфортной высоте → легко достать
- Контрастная табличка "Выход" → легко найти в экстренной ситуации

**Дверь B:**
- Плоская поверхность → непонятно, тянуть или толкать?
- Нет никаких указаний → приходится угадывать
- Ручка слишком высоко → неудобно для детей и низких людей
- Надпись серая на сером фоне → не видно

**Какая дверь лучше?** Очевидно, дверь A. Почему?

- **Usability** = насколько легко открыть дверь
- **Accessibility** = есть ли пандус для инвалидных колясок
- **Response Time** = как быстро дверь открывается после нажатия на кнопку автоматического открывания
- **Visual Hierarchy** = табличка "Выход" заметнее остальных элементов

> 💡 **Главный принцип UI/UX**: Если человеку нужна инструкция, чтобы воспользоваться дверью (или интерфейсом), — дизайн плохой.

**UI/UX и Usability требования** — это правила создания интерфейсов, которые работают как дверь A: интуитивно, удобно, доступно для всех.

---

## Что такое UI, UX и Usability?

### Простое объяснение

**UI (User Interface) — внешний вид**
> "Как выглядит дверь: цвет, форма ручки, материал"

**Примеры:**
- Цвет кнопки (синяя или зелёная)
- Размер шрифта (14px или 18px)
- Расположение меню (сверху или слева)
- Иконки (корзина для удаления, карандаш для редактирования)

**UX (User Experience) — общий опыт**
> "Насколько приятно и легко открыть дверь, насколько логично её расположение"

**Примеры:**
- Насколько быстро пользователь нашёл нужную функцию
- Пришлось ли ему думать где нажать
- Были ли неожиданности (кнопка сделала не то, что ожидалось)
- Остались ли приятные впечатления

**Usability (Удобство использования) — эффективность**
> "Могу ли я легко и быстро открыть дверь, даже если вижу её впервые"

**Измеряется конкретными метриками:**
- Новый пользователь выполнил задачу за < 3 минуты
- 90% пользователей нашли кнопку с первой попытки
- Менее 5% пользователей совершили ошибку

### Связь между UI, UX и Usability

```
UI (как выглядит)
    ↓
UX (как ощущается и работает)
    ↓
Usability (насколько легко использовать)
```

**Аналогия с рестораном:**
- **UI**: Оформление меню (шрифты, картинки блюд)
- **UX**: Весь опыт (как быстро подали, вкусно ли, приятно ли общались)
- **Usability**: Насколько легко выбрать блюдо и сделать заказ

---

## 5 компонентов Usability (Jakob Nielsen)

### 1. Learnability (Обучаемость)

**Определение:**
> "Насколько легко новому пользователю освоить систему впервые"

**Аналогия:**
> Как быстро гость в вашем доме найдёт туалет: если дверь обозначена табличкой и находится в логичном месте — learnability высокий.

**Пример требования:**
```
NFR-1: Новый пользователь должен совершить первую покупку за < 3 минуты без инструкций
NFR-2: 90% пользователей должны найти функцию поиска с первой попытки
```

**Как достичь:**
- Следовать конвенциям (логотип слева ведёт на главную)
- Добавить tooltips (подсказки при наведении)
- Онбординг для новых пользователей (короткий тур по функциям)

### 2. Efficiency (Эффективность)

**Определение:**
> "Насколько быстро опытный пользователь выполняет задачи"

**Аналогия:**
> Опытный водитель использует поворотники не глядя. Опытный пользователь использует Ctrl+C вместо правого клика → Копировать.

**Пример требования:**
```
NFR-3: Опытный пользователь должен создать заказ за < 30 секунд
NFR-4: Keyboard shortcuts для частых действий (Ctrl+S сохранить, Ctrl+F поиск)
```

**Как достичь:**
- Keyboard shortcuts
- Bulk actions (выделить все → удалить)
- Auto-complete, auto-fill
- Быстрый доступ к часто используемым функциям

### 3. Memorability (Запоминаемость)

**Определение:**
> "Насколько легко вспомнить как работать с системой после перерыва"

**Аналогия:**
> Вы не ездили на велосипеде 5 лет. Сможете ли сразу поехать? Если да — высокая memorability.

**Пример требования:**
```
NFR-5: Пользователь, не заходивший 1 месяц, должен выполнить знакомую задачу без переобучения
```

**Как достичь:**
- Consistent UI (единообразие во всей системе)
- Понятные метафоры (корзина = удаление, дискета = сохранение)
- Следовать платформенным стандартам (iOS, Android, Web)

### 4. Errors (Ошибки)

**Определение:**
> "Частота ошибок пользователя и лёгкость восстановления"

**Аналогия:**
> Gmail спрашивает "Вы забыли вложение?" если в письме есть слово "прикрепил", но нет файла — предотвращает ошибку.

**Пример требования:**
```
NFR-6: Критические действия (удаление) должны требовать подтверждения
NFR-7: Сообщения об ошибках должны объяснять что не так и как исправить
NFR-8: Должна быть возможность отменить (undo) последние 10 действий
```

**Как достичь:**
- Валидация форм в реальном времени
- Confirmation dialogs для критических действий
- Undo/Redo функциональность
- Понятные error messages ("Email должен содержать @" вместо "Неверный формат")

### 5. Satisfaction (Удовлетворённость)

**Определение:**
> "Насколько приятно работать с системой"

**Аналогия:**
> Два кафе с одинаковым меню. В одном официант грубый, в другом приветливый. Куда вернётесь? Satisfaction влияет на retention.

**Пример требования:**
```
NFR-9: Net Promoter Score (NPS) > 50
NFR-10: User Satisfaction Score > 4.0 из 5.0
```

**Как достичь:**
- Приятный визуальный дизайн
- Быстрая отзывчивость (perceived performance)
- Микровзаимодействия, анимации (кнопка "отпрыгивает" при нажатии)
- Отсутствие friction points (раздражающих моментов)

---

## Accessibility (Доступность)

### Что такое Accessibility?

**Определение:**
> "Интерфейс должен быть доступен для людей с ограниченными возможностями"

**Аналогия:**
> Пандус возле здания — это accessibility. Без него человек в инвалидной коляске не сможет войти, хотя здание красивое (UI) и внутри удобно (UX).

**Кто выигрывает от accessibility:**
- 👁️ Слабовидящие (screen readers, высокий контраст)
- 👂 Слабослышащие (субтитры для видео)
- 🖱️ Люди с двигательными нарушениями (keyboard navigation, большие кнопки)
- 👴 Пожилые (крупный шрифт, простой интерфейс)
- 🤕 Временные ограничения (сломанная рука → нужна клавиатурная навигация)

### WCAG (Web Content Accessibility Guidelines)

**Уровни соответствия:**
- **A** — минимальный уровень
- **AA** — рекомендуемый (стандарт для большинства сайтов)
- **AAA** — максимальный

**Пример требования:**
```
NFR-11: Интерфейс должен соответствовать WCAG 2.1 уровень AA
NFR-12: Вся функциональность доступна с клавиатуры (без мыши)
NFR-13: Цветовой контраст текста ≥ 4.5:1
```

### Принципы WCAG: POUR

**P — Perceivable (Воспринимаемый)**
> "Пользователь может воспринять контент"

Примеры:
- Альтернативный текст для изображений (screen reader прочитает)
- Субтитры для видео
- Достаточный цветовой контраст (текст виден на фоне)

**O — Operable (Управляемый)**
> "Пользователь может управлять интерфейсом"

Примеры:
- Навигация с клавиатуры (Tab, Enter, Space)
- Достаточно времени для чтения и реакции
- Нет мигающего контента (риск эпилепсии)

**U — Understandable (Понятный)**
> "Пользователь понимает контент и как его использовать"

Примеры:
- Понятный язык (не жаргон)
- Предсказуемое поведение (кнопка делает то, что написано)
- Помощь в исправлении ошибок ("Email должен содержать @")

**R — Robust (Надёжный)**
> "Работает с разными технологиями (assistive technologies)"

Примеры:
- Совместимость с screen readers (NVDA, JAWS)
- Правильные ARIA атрибуты
- Семантический HTML (<button> вместо <div onclick="">)

---

## Response Time Requirements (Требования ко времени отклика)

### Принципы Jakob Nielsen

**0.1 секунды — Мгновенная реакция**
> "Ощущается как прямое управление объектом"

**Примеры:**
- Hover эффект на кнопке
- Выделение текста
- Drag & drop движение

**1.0 секунда — Uninterrupted flow**
> "Пользователь не теряет фокус, но чувствует небольшую задержку"

**Примеры:**
- Загрузка страницы
- Submit формы
- Без loading indicator

**10 секунд — Предел внимания**
> "Дольше 10 секунд — пользователь переключится на другое"

**Примеры:**
- Сложные операции (генерация отчёта)
- Требуется loading indicator и progress bar
- Лучше предложить асинхронную обработку

**Аналогия:**
- **0.1s**: Выключатель света — мгновенно
- **1.0s**: Лифт приехал на этаж — терпимо
- **10s**: Ожидание в очереди — начинает раздражать

**Пример требования:**
```
NFR-14: Hover эффект должен срабатывать за < 100ms
NFR-15: Submit формы должен давать feedback за < 1 секунду
NFR-16: Операции > 3 секунд должны показывать progress indicator
```

---

## Mobile-First & Responsive Design

### Mobile-First подход

**Определение:**
> "Дизайн сначала для мобильных устройств, потом масштабирование на desktop"

**Аналогия:**
> Если вы смогли упаковать чемодан для ручной клади (mobile), то в большой чемодан (desktop) всё точно влезет. Но обратное не работает.

**Почему Mobile-First:**
- 60-80% трафика с мобильных
- Заставляет фокусироваться на главном (мало места)
- Проще добавить функции для большого экрана, чем урезать для маленького

**Пример требования:**
```
NFR-17: Интерфейс должен работать на экранах от 320px ширины
NFR-18: Touch targets (кнопки) должны быть ≥ 44×44 px (Apple HIG)
NFR-19: Вся функциональность доступна на мобильных
```

### Touch Targets (Размер кнопок на мобильных)

**Проблема:**
> Палец человека ~ 44px. Если кнопка 30×30 px → частые промахи → frustration

**Стандарты:**
- Apple Human Interface Guidelines: **≥ 44×44 px**
- Material Design (Google): **≥ 48×48 px**
- Spacing между кнопками: **≥ 8px**

**Плохо:**
```css
.button {
  width: 80px;
  height: 30px; /* Слишком маленькая! */
}
```

**Хорошо:**
```css
.button {
  min-width: 120px;
  min-height: 48px; /* Material Design стандарт */
  margin: 8px; /* Spacing между кнопками */
}
```

### Responsive Design (Адаптивный дизайн)

**Breakpoints (контрольные точки):**
- Mobile: < 768px
- Tablet: 768px - 1024px
- Desktop: > 1024px

**Пример требования:**
```
NFR-20: Layout должен адаптироваться под экраны 320px - 2560px
NFR-21: Тестирование на iOS Safari, Chrome Mobile, Samsung Internet
```

---

## Design Consistency (Единообразие дизайна)

### Что такое Design Consistency?

**Определение:**
> "Единообразие элементов, поведения и визуального стиля во всей системе"

**Аналогия:**
> Светофоры во всём мире: красный = стоп, зелёный = идти. Представьте, если бы в каждом городе были свои цвета — хаос!

**Типы:**
1. **Internal Consistency** — внутри вашего продукта (все кнопки одинаковые)
2. **External Consistency** — следование платформенным стандартам (iOS, Android, Web)

**Пример требования:**
```
NFR-22: Все primary кнопки должны использовать единый стиль
NFR-23: Цветовая палитра ограничена 5 основными цветами
NFR-24: Spacing между элементами кратен 8px (8px grid)
```

### Design System

**Что это:**
> "Библиотека переиспользуемых компонентов и правил дизайна"

**Компоненты:**
- **Design Tokens** (цвета, шрифты, spacing)
- **UI Components** (Button, Input, Card)
- **Patterns** (Forms, Navigation, Modals)
- **Guidelines** (когда использовать что)

**Примеры Design Systems:**
- Material Design (Google)
- Apple Human Interface Guidelines
- Fluent Design (Microsoft)
- Cloudscape (AWS)

---

## Visual Hierarchy (Визуальная иерархия)

### Что такое Visual Hierarchy?

**Определение:**
> "Организация элементов по важности через размер, цвет, позицию, контраст"

**Аналогия:**
> Газета: заголовок крупнее текста, важные новости сверху, реклама мельче и сбоку. Глаза сразу идут к важному.

**Принципы:**
- **Size** (размер) — важное крупнее
- **Color** (цвет) — акценты для важного
- **Position** (позиция) — важное выше/левее (F-pattern)
- **Contrast** (контраст) — важное контрастнее

**Пример требования:**
```
NFR-25: Primary CTA (Call-to-Action) должен быть самой заметной кнопкой на странице
NFR-26: Заголовки страниц H1 крупнее основного текста в 2 раза
```

### F-Pattern (F-паттерн чтения)

**Исследование eye-tracking:**
> Пользователи читают веб-страницы в форме буквы F:
> 1. Горизонтально сверху (заголовок)
> 2. Немного ниже горизонтально (подзаголовок)
> 3. Вертикально слева (сканирование)

**Вывод:** Размещайте важное слева и сверху.

---

## Feedback & Affordance

### Feedback (Обратная связь)

**Определение:**
> "Система сообщает пользователю о результате действия"

**Аналогия:**
> Вы нажали кнопку звонка в дверь. Если не слышите звука внутри — непонятно, работает ли звонок. Feedback = звук "динь-донь".

**Типы feedback:**
1. **Immediate** (мгновенный) — hover, focus, click
2. **Progress** (процесс) — loading indicators, progress bars
3. **Completion** (завершение) — success/error messages

**Пример требования:**
```
NFR-27: Каждое действие пользователя должно давать визуальный feedback < 100ms
NFR-28: Success/Error messages должны исчезать через 5 секунд или по клику
```

**Плохо (нет feedback):**
```javascript
function handleSubmit() {
  api.post('/form', data); // Пользователь не знает что происходит!
}
```

**Хорошо (с feedback):**
```javascript
function handleSubmit() {
  setLoading(true); // Loading state
  api.post('/form', data)
    .then(() => showToast('Успешно сохранено!')) // Success feedback
    .catch(() => showError('Ошибка. Попробуйте снова.')) // Error feedback
    .finally(() => setLoading(false));
}
```

### Affordance (Подсказки использования)

**Определение:**
> "Визуальные подсказки как использовать элемент"

**Аналогия:**
> Кнопка на микроволновке выпуклая → понятно, что её нужно нажать. Плоская поверхность → непонятно, кнопка ли это.

**Примеры good affordance:**
- Кнопка выглядит "нажимаемой" (тень, градиент, скруглённые углы)
- Ссылка подчёркнута или цветная (отличается от текста)
- Input field имеет border (показывает куда вводить)
- Slider имеет ручку для перетаскивания

**Примеры bad affordance:**
- Текст выглядит как кнопка (но не кликабельный)
- Кнопка выглядит как текст (не понятно что кликабельно)
- Нет визуального отличия между активной и неактивной кнопкой

---

## Forms Design (Дизайн форм)

### Best Practices (Лучшие практики)

#### 1. Label Position (Расположение подписей)

**Лучше всего:** Сверху input (top-aligned)

**Почему:**
- Быстрее сканирование формы (взгляд движется сверху вниз)
- Label всегда виден (не исчезает при вводе)
- Подходит для длинных labels

**Плохо:** Label внутри input (placeholder вместо label)

**Проблема:**
- Placeholder исчезает при вводе → пользователь забывает что вводил
- Screen readers могут не читать placeholder
- Low contrast placeholder (серый) плохо видно

**Пример:**

❌ **Неправильно:**
```html
<input type="email" placeholder="Enter your email">
```

✅ **Правильно:**
```html
<label for="email">Email</label>
<input id="email" type="email" placeholder="example@email.com">
```

#### 2. Inline Validation (Валидация в реальном времени)

**Когда срабатывать:** onBlur (потеря фокуса)

**Почему не onChange (при каждом символе):**
- Раздражает (показывает ошибку пока пользователь ещё вводит)
- "Email invalid" когда пользователь ввёл только "a" — слишком рано

**Хорошо:**
```javascript
function handleEmailBlur() {
  if (!validateEmail(email)) {
    setEmailError('Email должен содержать @');
  } else {
    setEmailError(null);
    setEmailSuccess(true); // Зелёная галочка ✓
  }
}
```

#### 3. Error Messages (Сообщения об ошибках)

**Где показывать:** Рядом с полем (не вверху формы)

**Как формулировать:** Конкретно (что не так и как исправить)

❌ **Плохо:** "Неверный формат"
✅ **Хорошо:** "Email должен содержать символ @"

❌ **Плохо:** "Ошибка валидации"
✅ **Хорошо:** "Пароль должен быть минимум 8 символов"

---

## Loading States & Empty States

### Loading States (Состояние загрузки)

#### Spinner vs Skeleton Screens

**Spinner (крутящийся индикатор):**
```
[Пустой экран с крутящимся кружком в центре]
"Loading..."
```
**Perceived load time:** Кажется дольше

**Skeleton Screens (скелетоны):**
```
[Серые блоки на месте будущих постов]
[Avatar placeholder] [Text lines placeholder]
[Avatar placeholder] [Text lines placeholder]
```
**Perceived load time:** Кажется быстрее!

**Почему Skeleton лучше:**
1. **Предсказуемость** — пользователь видит структуру контента (знает что ждать)
2. **Perceived performance** — создаёт иллюзию быстрее загрузки
3. **Меньше frustration** — не просто "крутится", а "почти загрузилось"

**Когда использовать Spinner:**
- Неизвестна структура контента (например, после form submit)
- Очень быстрая операция (< 300ms — skeleton не успеет показаться)

**Пример требования:**
```
NFR-29: Операции > 1 секунды должны показывать loading spinner
NFR-30: Загрузка списков должна использовать skeleton screens
```

### Empty States (Пустое состояние)

**Плохо:**
```
[Пустой экран]
"Нет данных"
```

**Проблема:** Пользователь confused — "Что делать дальше?"

**Хорошо:**
```
[Иллюстрация: пустой inbox]

"У вас пока нет задач"
"Создайте первую задачу, чтобы начать"

[Кнопка: + Создать задачу]

Подсказки:
• Нажмите Ctrl+N для быстрого добавления
• Перетаскивайте задачи для изменения порядка
• Нажмите ☆ чтобы отметить как важное
```

**Компоненты Good Empty State:**
1. **Illustration** (иллюстрация) — визуальный контекст
2. **Explanation** (объяснение) — почему пусто
3. **CTA (Call-to-Action)** — что делать дальше
4. **Tips** (подсказки) — как использовать систему

---

## Метрики Usability

### Количественные метрики

**1. Task Success Rate (Процент успешного выполнения задачи)**
```
% пользователей, успешно выполнивших задачу
Target: > 90%
```

**2. Time on Task (Время на задачу)**
```
Среднее время выполнения задачи
Target: < 2 минут для регистрации
```

**3. Error Rate (Частота ошибок)**
```
% пользователей, совершивших ошибку
Target: < 5%
```

**4. Clicks to Complete (Количество кликов)**
```
Количество кликов для выполнения задачи
Target: ≤ 3 клика до покупки
```

### Качественные метрики

**1. System Usability Scale (SUS)**
- Стандартный опросник 10 вопросов
- Шкала 0-100
- Target: SUS > 68 (выше среднего)

**2. Net Promoter Score (NPS)**
- "Порекомендуете ли вы продукт друзьям?"
- Шкала -100 до +100
- Target: NPS > 50 (отлично)

**3. Customer Satisfaction (CSAT)**
- "Насколько вы удовлетворены?"
- Шкала 1-5
- Target: CSAT > 4.0

---

## Практические сценарии

### Сценарий 1: Usability Testing выявил проблему с навигацией

**Ситуация:** E-commerce сайт. Conversion rate низкий (2%). Проводим usability testing с 5 пользователями.

**Метод:** Think Aloud Protocol + Task-Based Testing

**Задача:** "Найдите и купите синюю мужскую рубашку размера M"

**Наблюдения:**
- **User 1:** "Где категории? Не вижу меню мужской одежды" (застрял на 2 минуты)
- **User 2:** Нашёл через поиск, но фильтры непонятны
- **User 3:** "Кнопка 'Купить' слишком маленькая, промахнулся 3 раза на mobile"
- **User 4:** Добавил в корзину, но не нашёл как оформить заказ
- **User 5:** Успешно, но заняло 8 минут (target: < 3 минуты)

**Task Success Rate:** 1/5 = 20% (target: > 90%)

**Проблемы выявлены:**
1. Главное меню скрыто в hamburger icon (плохая learnability)
2. Touch targets кнопок < 30×30 px (нарушение Apple HIG 44×44 px)
3. Checkout button не заметен (плохая visual hierarchy)

**Решение:**
- Главное меню всегда видимо (desktop: horizontal nav, mobile: sticky bottom nav)
- Touch targets увеличить до 48×48 px
- Checkout button сделать primary CTA (зелёный, крупный, контрастный)

**Retest:** Task Success Rate = 95%, Time on Task = 2 минуты

**Объяснение:** 5 пользователей нашли 85% проблем (Jakob Nielsen). Usability testing ДО запуска → избежали низкого conversion в production.

> 💡 **Важно помнить:** Test early, test often. Фиксить проблемы в design фазе дешевле чем в production (10x-100x дешевле).

---

### Сценарий 2: WCAG Compliance для государственного сайта

**Ситуация:** Сайт муниципальных услуг должен соответствовать WCAG 2.1 уровень AA (законодательное требование).

**Аудит через WAVE tool выявил нарушения:**

**1. Цветовой контраст недостаточен**

❌ **Проблема:**
```css
color: #999; /* Серый */
background: #FFF; /* Белый */
```
Контраст: 2.8:1 (нужно ≥ 4.5:1)

✅ **Решение:**
```css
color: #767676; /* Более тёмный серый */
background: #FFF;
```
Контраст: 4.54:1 ✓

**2. Изображения без alt text**

❌ **Проблема:**
```html
<img src="form.png">
```
Screen reader не понимает что на изображении.

✅ **Решение:**
```html
<img src="form.png" alt="Форма заявления на получение паспорта">
```

**3. Keyboard navigation не работает**

❌ **Проблема:** Tab key пропускает dropdown menu

✅ **Решение:** Добавить tabindex, aria-expanded, focus management

**4. Form inputs без labels**

❌ **Проблема:**
```html
<input type="text" placeholder="ИНН">
```
Screen reader не читает placeholder.

✅ **Решение:**
```html
<label for="inn">ИНН</label>
<input id="inn" type="text">
```

**5. Недостаточно времени для чтения**

❌ **Проблема:** Auto-rotating carousel меняется каждые 3 секунды

✅ **Решение:** Добавить паузу, увеличить до 10 секунд, кнопки навигации

**Тестирование с NVDA screen reader:**
- **До:** Пользователь с нарушением зрения не смог заполнить форму
- **После:** Успешно заполнил и отправил заявление

**Объяснение:** WCAG compliance — не nice-to-have, а legal requirement для госсайтов. Accessibility = inclusivity, 15% населения с ограниченными возможностями.

> 💡 **Важно помнить:** Accessibility помогает не только людям с инвалидностью, но и пожилым (плохое зрение), пользователям с временными ограничениями (сломанная рука), keyboard-only users.

---

### Сценарий 3: Mobile-First для приложения доставки еды

**Ситуация:** 80% пользователей заходят с mobile. Desktop-first дизайн не работает на маленьких экранах.

**Проблема Desktop-First:**
```
Desktop (1920px) дизайн:
- 3 колонки ресторанов
- Sidebar с фильтрами слева
- Большие карточки товаров

Адаптация на mobile (375px):
- 3 колонки → всё крошечное (нечитаемо)
- Sidebar перекрывает контент
- Touch targets overlap (случайные клики)
```

**Mobile-First подход:**

**1. Начать с mobile (375px):**
- 1 колонка ресторанов
- Фильтры в bottom sheet (swipe up)
- Touch targets ≥ 48×48 px, spacing ≥ 8px между кнопками

**2. Progressive enhancement для tablet (768px):**
- 2 колонки ресторанов
- Фильтры в sidebar справа

**3. Desktop (1024px+):**
- 3-4 колонки
- Фильтры слева постоянно видимы

**Результат:**
- Mobile bounce rate: 60% → 15%
- Mobile conversion: 1% → 5%
- Time on Task (заказ): 8 минут → 2 минуты

**Объяснение:** Mobile-First = дизайн для самого ограниченного экрана первым. Заставляет фокусироваться на главном, убирать лишнее. Desktop получает enhanced version, не crammed version.

> 💡 **Важно помнить:** Mobile-First ≠ "mobile only". Это подход к дизайну: начать с constraints (малый экран) → добавить возможности для больших экранов.

---

### Сценарий 4: Touch Targets меньше 44px на мобильной версии сайта

**Ситуация:** Пользователи жалуются на мобильной версии: "Постоянно нажимаю не на ту кнопку!"

**Проблема:**
```css
.delete-button {
  width: 30px;
  height: 30px; /* Слишком маленькая для пальца! */
}

.edit-button {
  width: 30px;
  height: 30px;
  margin-left: 2px; /* Кнопки слишком близко! */
}
```

**Анализ:**
- Палец человека ~ 44px
- Кнопки 30×30 px → промахи
- Spacing 2px → соседние кнопки перекрываются при нажатии

**Решение:**
```css
.delete-button,
.edit-button {
  min-width: 48px; /* Material Design стандарт */
  min-height: 48px;
  margin: 8px; /* Spacing между targets */
}
```

**Тестирование:**
- **До:** 35% пользователей промахивались по кнопке с первой попытки
- **После:** 5% пользователей промахивались (остались те, кто случайно промахнулся)

**Объяснение:** Apple HIG требует ≥ 44×44 px, Material Design ≥ 48×48 px. Маленькие touch targets → frustration и accessibility нарушение (пожилые, люди с motor impairments не могут попасть).

> 💡 **Важно помнить:** Всегда тестируйте на реальных устройствах, а не только в Chrome DevTools. Реальный палец ≠ курсор мыши.

---

### Сценарий 5: Placeholder вместо Label — типичная ошибка

**Ситуация:** Форма регистрации. Label отсутствует, вся информация в placeholder.

❌ **Плохая реализация:**
```html
<input type="email" placeholder="Введите ваш email">
<input type="password" placeholder="Минимум 8 символов">
```

**Проблемы:**
1. **Placeholder исчезает при вводе** → пользователь забывает что вводил
2. **Screen readers могут не читать placeholder** → accessibility нарушение
3. **Low contrast placeholder** (обычно серый #999) → плохо видно

**Тестирование:**
- Пользователь начал вводить пароль
- Placeholder исчез
- Пользователь забыл требование "минимум 8 символов"
- Ввёл "12345", submit → ошибка
- Frustration

✅ **Правильная реализация:**
```html
<label for="email">Email</label>
<input id="email" type="email" placeholder="example@example.com">

<label for="password">Пароль</label>
<input id="password" type="password" placeholder="Минимум 8 символов">
<small>Пароль должен содержать минимум 8 символов</small>
```

**Почему лучше:**
- **Label всегда видим** (не исчезает)
- **Placeholder = пример формата** (example@example.com)
- **Hint text (small)** = дополнительная подсказка
- **Screen reader читает label + hint**

**Объяснение:** Label ≠ Placeholder. Label = что это поле, Placeholder = пример формата. Никогда не используйте placeholder вместо label.

> 💡 **Важно помнить:** Если убрать placeholder — форма должна остаться понятной. Если непонятно — вы использовали placeholder неправильно.

---

### Сценарий 6: Нет Feedback при отправке формы

**Ситуация:** Пользователь нажал "Отправить", но не понимает что происходит.

❌ **Плохая реализация:**
```javascript
function handleSubmit() {
  api.post('/contact', formData); // Нет feedback!
}
```

**Проблемы:**
1. Пользователь не знает что происходит (отправляется? застряло?)
2. Multiple clicks → duplicate submits (нажимает несколько раз)
3. Если медленная сеть → пользователь думает "не работает" и уходит

**Тестирование:**
- Пользователь нажал "Отправить"
- 5 секунд тишины (медленная сеть)
- Пользователь нажал ещё раз (дубликат!)
- Пользователь закрыл страницу (ушёл)

✅ **Правильная реализация:**
```javascript
function handleSubmit() {
  setLoading(true); // 1. Loading state
  setButtonDisabled(true); // 2. Prevent double-click

  api.post('/contact', formData)
    .then(() => {
      showToast('Сообщение отправлено!', 'success'); // 3. Success feedback
      resetForm();
    })
    .catch((error) => {
      showToast('Ошибка отправки. Попробуйте снова.', 'error'); // 4. Error feedback
    })
    .finally(() => {
      setLoading(false);
      setButtonDisabled(false);
    });
}
```

**UI:**
```
[До нажатия]
[Кнопка: Отправить]

[После нажатия, loading]
[Кнопка disabled с spinner: Отправка...]

[Success]
[Toast уведомление: ✓ Сообщение отправлено!]

[Error]
[Toast уведомление: ✗ Ошибка. Попробуйте снова.]
```

**Объяснение:** Every user action needs feedback. Immediate (hover, click), Progress (loading), Completion (success/error). Без feedback пользователь в замешательстве.

> 💡 **Важно помнить:** Feedback < 100ms для immediate actions. Feedback < 1 секунду для форм. Операции > 3 секунды показывать progress indicator.

---

### Сценарий 7: Low Contrast Text (Низкий контраст текста)

**Ситуация:** Дизайнер выбрал светло-серый текст на белом фоне для "минималистичного" дизайна.

❌ **Проблема:**
```css
.text {
  color: #AAAAAA; /* Светло-серый */
  background: #FFFFFF; /* Белый */
}
```

**Проверка контраста:**
- Контраст: 2.32:1
- WCAG AA требует: ≥ 4.5:1
- **Fail!** ❌

**Последствия:**
1. Люди с плохим зрением не могут прочитать
2. На ярком солнце (mobile outdoor) нечитаемо
3. Пожилые пользователи жалуются "не вижу текст"
4. Compliance violation (нарушение WCAG 2.1 AA)

✅ **Решение:**
```css
.text {
  color: #595959; /* Более тёмный серый */
  background: #FFFFFF;
}
```

**Проверка контраста:**
- Контраст: 7.0:1
- WCAG AA: ✓ Pass
- WCAG AAA: ✓ Pass (bonus!)

**Инструменты для проверки:**
- WebAIM Contrast Checker
- Chrome DevTools (Accessibility panel)
- Figma Contrast Plugin

**WCAG требования:**
- Normal text (< 18pt): ≥ 4.5:1
- Large text (≥ 18pt или ≥ 14pt bold): ≥ 3:1

**Объяснение:** Accessibility = не только screen readers, но и контраст для всех пользователей. 15%+ людей имеют проблемы со зрением.

> 💡 **Важно помнить:** "Красивый" дизайн бесполезен, если текст нечитаемый. Always check contrast before shipping.

---

### Сценарий 8: Только мышь, без клавиатуры — Accessibility Fail

**Ситуация:** Web приложение. Compliance audit выявил: keyboard navigation не работает.

**Проблема:**
- Modal dialog открывается, но нельзя закрыть через Esc key
- Dropdown menu не открывается через Enter/Space
- Focus trap отсутствует в modal (Tab выходит за пределы modal)

**Тестирование keyboard-only:**

**Задача:** "Создать новый проект используя только клавиатуру (no mouse)"

**Попытка 1 (до фикса):**
```
1. Tab to "New Project" button → ✅ works
2. Press Enter → Modal opens ✅
3. Tab to "Project Name" field → ❌ Focus jumps outside modal!
4. Try Esc to close modal → ❌ Doesn't work
5. Stuck, can't proceed
```

**Task Success Rate:** 0% (blocked)

**Фиксы:**

**1. Focus Management:**
```javascript
function openModal() {
  setIsOpen(true);
  // Focus first input when modal opens
  setTimeout(() => modal.current?.querySelector('input')?.focus(), 0);
}

function closeModal() {
  setIsOpen(false);
  // Return focus to trigger button
  triggerButton.current?.focus();
}
```

**2. Focus Trap (Tab stays inside modal):**
```javascript
function handleTabKey(e) {
  const focusableElements = modal.current.querySelectorAll(
    'button, input, select, textarea, [tabindex]:not([tabindex="-1"])'
  );
  const first = focusableElements[0];
  const last = focusableElements[focusableElements.length - 1];

  if (e.shiftKey && document.activeElement === first) {
    e.preventDefault();
    last.focus();
  } else if (!e.shiftKey && document.activeElement === last) {
    e.preventDefault();
    first.focus();
  }
}
```

**3. Escape Key:**
```javascript
useEffect(() => {
  function handleEscape(e) {
    if (e.key === 'Escape') closeModal();
  }
  document.addEventListener('keydown', handleEscape);
  return () => document.removeEventListener('keydown', handleEscape);
}, []);
```

**Попытка 2 (после фикса):**
```
1. Tab to "New Project" button → ✅
2. Enter → Modal opens, focus on first input ✅
3. Fill form with Tab navigation ✅
4. Tab stays within modal (focus trap) ✅
5. Esc closes modal, focus returns to button ✅
```

**Task Success Rate:** 100% ✓

**Объяснение:** Keyboard navigation critical для accessibility (screen reader users, motor impairments) и power users. WCAG 2.1 требует full keyboard support.

> 💡 **Важно помнить:** Test with keyboard only regularly. If you can't complete task with keyboard → accessibility fail. Affects 15%+ users.

---

### Сценарий 9: A/B Testing для оптимизации conversion

**Ситуация:** E-commerce checkout page. Conversion rate 45% (55% abandon cart). Хотим улучшить.

**Hypothesis:** Упрощение checkout повысит conversion.

**Variant A (Control — текущий):**
```
Step 1: Shipping address (5 полей)
Step 2: Billing address (5 полей)
Step 3: Payment method
Step 4: Review order
Total: 4 steps, 12 полей
```

**Variant B (Test — упрощённый):**
```
Single page checkout:
- Shipping address (5 полей)
- [Checkbox: "Billing same as shipping"] (auto-fill)
- Payment method
- Order summary (sidebar)
Total: 1 step, 7 полей
```

**A/B Test (2 недели, 10,000 users each):**

**Результаты:**
```
Variant A (Control):
- Conversion rate: 45%
- Time to complete: 4 минуты
- Cart abandonment: 55%

Variant B (Test):
- Conversion rate: 62% (+17 percentage points!)
- Time to complete: 2 минуты
- Cart abandonment: 38%
```

**Statistical significance:** p < 0.01 (highly significant)

**Решение:** Rollout Variant B to 100% users.

**Revenue impact:**
- 10,000 checkouts/month × 17% increase = +1,700 orders/month
- Average order $50 → +$85,000/month revenue

**Объяснение:** Меньше полей + меньше steps = меньше friction = выше conversion. A/B testing доказал гипотезу с данными.

> 💡 **Важно помнить:** A/B test your assumptions. "I think users prefer X" < "Data shows users prefer X". Всегда measure impact.

---

### Сценарий 10: System Usability Scale (SUS) для оценки продукта

**Ситуация:** B2B SaaS продукт после релиза. Нужно измерить usability.

**Метод:** System Usability Scale (SUS) опросник

**10 вопросов** (1-5 scale: Strongly Disagree to Strongly Agree):
1. Я бы использовал эту систему регулярно
2. Система излишне сложная
3. Система проста в использовании
4. Мне нужна техподдержка для использования
5. Функции хорошо интегрированы
6. Слишком много несоответствий
7. Большинство людей научатся быстро
8. Система громоздкая
9. Я чувствую уверенность используя систему
10. Мне нужно много учить перед использованием

**Результаты (50 пользователей):**
- Average SUS Score: 52 / 100

**Интерпретация:**
- < 50: Fail (плохо)
- 50-70: OK (приемлемо, но нужны улучшения)
- 70-85: Good (хорошо)
- > 85: Excellent (отлично)

**52 = OK, но нужны улучшения**

**Проблемы выявлены:**
- Question 2 (сложность): 4.2/5 → "Система излишне сложная" (плохо!)
- Question 4 (поддержка): 3.8/5 → "Нужна техподдержка" (плохо!)

**Улучшения:**
1. Добавить onboarding tour для new users
2. Упростить navigation (уменьшить клики с 5 до 2 для частых задач)
3. Inline help tooltips для сложных функций

**Retest через 3 месяца:**
- SUS Score: 52 → 76 (Good!)

**Объяснение:** SUS — стандартизированный метод измерения usability. Позволяет track improvements over time и сравнивать с конкурентами.

> 💡 **Важно помнить:** SUS быстрый (2 минуты) и валидированный. Проводите каждые 3-6 месяцев для tracking usability improvements.

---

## Типичные ошибки — Чего избегать

### Ошибка 1: Placeholder вместо Label

❌ **Неправильно:**
```html
<input type="email" placeholder="Enter your email">
```

**Проблема:**
- Placeholder исчезает при вводе
- Screen readers могут не читать
- Low contrast (серый) плохо видно

✅ **Правильно:**
```html
<label for="email">Email</label>
<input id="email" type="email" placeholder="example@email.com">
```

> 💡 **Совет:** Label = what field is. Placeholder = example format. Never use placeholder as label.

---

### Ошибка 2: Touch Targets < 44×44 px на mobile

❌ **Неправильно:**
```css
.button {
  width: 80px;
  height: 30px; /* Too small! */
}
```

**Проблема:**
- Палец ~44px, кнопка 30px → промахи
- Frustration: user тапает несколько раз

✅ **Правильно:**
```css
.button {
  min-width: 120px;
  min-height: 48px; /* Material Design standard */
  margin: 8px; /* Spacing between targets */
}
```

> 💡 **Совет:** Apple HIG: ≥ 44×44 px. Material Design: ≥ 48×48 px. Use the larger for better accessibility.

---

### Ошибка 3: Цветовой контраст < 4.5:1

❌ **Неправильно:**
```css
.text {
  color: #999; /* Light gray */
  background: #FFF; /* White */
}
```
Контраст: 2.8:1 (WCAG fail!)

✅ **Правильно:**
```css
.text {
  color: #767676; /* Darker gray */
  background: #FFF;
}
```
Контраст: 4.54:1 (WCAG AA pass!)

> 💡 **Совет:** Normal text ≥ 4.5:1, Large text (≥18pt) ≥ 3:1. Always check contrast before shipping.

---

### Ошибка 4: Нет Feedback при действиях пользователя

❌ **Неправильно:**
```javascript
function handleSubmit() {
  api.post('/form', data); // No feedback!
}
```

**Проблема:**
- User не знает что происходит
- Multiple clicks → duplicate submits

✅ **Правильно:**
```javascript
function handleSubmit() {
  setLoading(true); // Loading state
  api.post('/form', data)
    .then(() => showToast('Success!'))
    .catch(() => showError('Failed. Try again.'))
    .finally(() => setLoading(false));
}
```

> 💡 **Совет:** Every user action needs feedback. Immediate (hover), Progress (loading), Completion (success/error).

---

### Ошибка 5: Только мышь, без клавиатуры

❌ **Неправильно:**
```javascript
<div onclick="handleClick()">Click me</div>
```

**Проблема:**
- Keyboard users не могут нажать (no Tab, no Enter)
- Screen reader users застряли

✅ **Правильно:**
```javascript
<button onClick={handleClick} onKeyDown={handleKeyboard}>
  Click me
</button>
```

> 💡 **Совет:** Always use semantic HTML (<button>, <a>) вместо <div onclick="">. Test with keyboard only.

---

### Ошибка 6: Формы без inline validation

❌ **Неправильно:**
```javascript
// Validation только при submit
function handleSubmit() {
  if (!validateEmail(email)) {
    alert('Invalid email!'); // После заполнения всей формы!
  }
}
```

**Проблема:**
- User заполняет всю форму → submit → "Email invalid" → frustration

✅ **Правильно:**
```javascript
// Inline validation на onBlur
function handleEmailBlur() {
  if (!validateEmail(email)) {
    setEmailError('Email должен содержать @');
  } else {
    setEmailSuccess(true); // Green checkmark ✓
  }
}
```

> 💡 **Совет:** Validate onBlur (потеря фокуса), не onChange (while typing раздражает). Show success (✓) and errors (✗) inline.

---

### Ошибка 7: Desktop-only testing

❌ **Неправильно:**
QA: "Tested on my laptop (1920×1080). Looks good! Ship it!"

Production: 60% mobile users → layout broken, buttons overlap, текст обрезан.

✅ **Правильно:**
Test на:
- Mobile (375×667, 414×896)
- Tablet (768×1024)
- Desktop (1920×1080)
- Chrome DevTools Device Mode
- Real devices (iOS, Android)

> 💡 **Совет:** Mobile-first testing. If it works on mobile (hardest), it works on desktop. Reverse not true.

---

### Ошибка 8: Ignoring Empty States

❌ **Неправильно:**
```javascript
{items.length === 0 ? (
  <div>No items</div>
) : (
  <ItemList items={items} />
)}
```

**Проблема:**
- "No items" не помогает user (что делать?)

✅ **Правильно:**
```javascript
{items.length === 0 ? (
  <EmptyState
    illustration={<EmptyIcon />}
    title="No items yet"
    description="Create your first item to get started"
    action={<Button onClick={onCreate}>+ Create Item</Button>}
  />
) : (
  <ItemList items={items} />
)}
```

> 💡 **Совет:** Empty State = opportunity for onboarding. Guide user to first action.

---

## Заключение

**UI/UX и Usability требования** критичны для успеха продукта.

### Запомните главное:

**5 компонентов Usability (Jakob Nielsen):**
1. **Learnability** — легко освоить
2. **Efficiency** — быстро выполнять задачи
3. **Memorability** — легко вспомнить после перерыва
4. **Errors** — минимум ошибок, легко восстановиться
5. **Satisfaction** — приятно использовать

**Accessibility (WCAG 2.1 AA):**
- Keyboard navigation
- Screen reader support
- Цветовой контраст ≥ 4.5:1

**Response Time (Jakob Nielsen):**
- 0.1s — мгновенно (hover)
- 1.0s — uninterrupted flow (загрузка страницы)
- 10s — предел внимания (progress indicator)

**Mobile-First:**
- Touch targets ≥ 44×44 px (Apple HIG)
- Touch targets ≥ 48×48 px (Material Design)
- Spacing ≥ 8px

**Design Consistency:**
- Design System
- Единообразие элементов

**Testing:**
- Минимум 5 пользователей найдут 85% проблем (Jakob Nielsen)
- Test early, test often

> 🎯 **Золотое правило:** "Don't make me think" — Steve Krug. Интерфейс должен быть интуитивным!
`;

const questions = [
  {
    question: "В чём разница между UI и UX?",
    options: [
      "Нет разницы, это синонимы",
      "UI — как выглядит (визуальный дизайн), UX — как работает и ощущается (общий опыт)",
      "UI для desktop, UX для mobile",
      "UI делает дизайнер, UX делает разработчик"
    ],
    correctAnswer: 1,
    explanation: "UI (User Interface) — визуальный дизайн интерфейса: кнопки, цвета, типографика, layout (фокус: как выглядит). UX (User Experience) — общий опыт пользователя при взаимодействии с продуктом: удобство, логичность, эмоции (фокус: как работает, как ощущается). UI — часть UX."
  },
  {
    question: "Какие 5 компонентов Usability выделил Jakob Nielsen?",
    options: [
      "Speed, Security, Scalability, Stability, Satisfaction",
      "Learnability, Efficiency, Memorability, Errors, Satisfaction",
      "Design, Development, Deployment, Documentation, Debugging",
      "Frontend, Backend, Database, API, UI"
    ],
    correctAnswer: 1,
    explanation: "5 компонентов Usability (Jakob Nielsen): 1) Learnability (обучаемость — легко освоить), 2) Efficiency (эффективность — быстро выполнять задачи), 3) Memorability (запоминаемость — легко вспомнить после перерыва), 4) Errors (ошибки — минимум ошибок, легко восстановиться), 5) Satisfaction (удовлетворённость — приятно использовать)."
  },
  {
    question: "Что такое Learnability (обучаемость)?",
    options: [
      "Скорость загрузки приложения",
      "Насколько легко новым пользователям освоить систему",
      "Количество функций в системе",
      "Скорость выполнения задач опытными пользователями"
    ],
    correctAnswer: 1,
    explanation: "Learnability (обучаемость) — насколько легко новым пользователям освоить систему. Пример требования: новый пользователь должен совершить первую покупку за < 3 минуты без инструкций. Принципы: понятная навигация, tooltips, онбординг туры, следование конвенциям (логотип слева ведёт на главную)."
  },
  {
    question: "Что такое WCAG и для чего нужен?",
    options: [
      "Инструмент для дизайна",
      "Web Content Accessibility Guidelines — стандарт доступности для людей с ограниченными возможностями",
      "Framework для frontend",
      "Библиотека компонентов"
    ],
    correctAnswer: 1,
    explanation: "WCAG (Web Content Accessibility Guidelines) — стандарт доступности веб-контента для людей с ограниченными возможностями. Уровни: A (минимальный), AA (рекомендуемый стандарт), AAA (максимальный). Требование: интерфейс должен соответствовать WCAG 2.1 уровень AA. Включает: keyboard navigation, screen reader support, цветовой контраст ≥ 4.5:1."
  },
  {
    question: "Что означают принципы WCAG: POUR?",
    options: [
      "Performance, Optimization, Usability, Reliability",
      "Perceivable, Operable, Understandable, Robust",
      "Planning, Operation, Update, Release",
      "Privacy, Ownership, Usage, Rights"
    ],
    correctAnswer: 1,
    explanation: "POUR принципы WCAG: 1) Perceivable (воспринимаемый) — альтернативный текст для изображений, субтитры, цветовой контраст, 2) Operable (управляемый) — навигация с клавиатуры, нет мигающего контента, 3) Understandable (понятный) — понятный язык, предсказуемое поведение, 4) Robust (надёжный) — совместимость с assistive technologies (screen readers)."
  },
  {
    question: "Какие требования Jakob Nielsen к response time для UI?",
    options: [
      "Все операции должны быть < 1 секунды",
      "0.1s (мгновенно), 1.0s (uninterrupted flow), 10s (предел внимания с progress indicator)",
      "Все операции должны быть < 100ms",
      "Нет требований к response time"
    ],
    correctAnswer: 1,
    explanation: "Jakob Nielsen response time для UI: 1) 0.1 секунды — мгновенная реакция (hover, выделение текста), 2) 1.0 секунда — предел для uninterrupted flow (загрузка страницы, submit формы без loading indicator), 3) 10 секунд — предел удержания внимания (сложные операции, требуется loading indicator и progress bar)."
  },
  {
    question: "Что такое Mobile-First подход?",
    options: [
      "Разработка только для мобильных",
      "Дизайн сначала для мобильных устройств, потом масштабирование на desktop",
      "Мобильные важнее desktop",
      "Тестирование только на мобильных"
    ],
    correctAnswer: 1,
    explanation: "Mobile-First — дизайн сначала для мобильных устройств (минимальный экран 320px), потом масштабирование на desktop. Требования: интерфейс работает на экранах от 320px, touch targets (кнопки) ≥ 44×44 px (Apple HIG), вся функциональность доступна на мобильных. Преимущества: фокус на важном, лучше performance."
  },
  {
    question: "Какой минимальный размер touch targets (кнопок) рекомендуют Apple HIG и Material Design?",
    options: [
      "Apple: 44×44 px, Material Design: 48×48 px",
      "Apple: 32×32 px, Material Design: 32×32 px",
      "Apple: 64×64 px, Material Design: 64×64 px",
      "Нет минимума"
    ],
    correctAnswer: 0,
    explanation: "Минимальный размер touch targets: Apple HIG рекомендует ≥ 44×44 px, Material Design ≥ 48×48 px. Spacing между кнопками ≥ 8px. Почему важно: палец пользователя ~44px, меньшие кнопки сложно нажать (частые промахи). Требование: NFR-21: Touch targets должны быть ≥ 44×44 px."
  },
  {
    question: "Что такое Design Consistency?",
    options: [
      "Красивый дизайн",
      "Единообразие элементов, поведения и визуального стиля во всей системе",
      "Использование одного цвета",
      "Одинаковый размер всех кнопок"
    ],
    correctAnswer: 1,
    explanation: "Design Consistency — единообразие элементов, поведения и визуального стиля во всей системе. Типы: Internal Consistency (внутри продукта), External Consistency (следование платформенным стандартам iOS/Android/Web). Пример требования: все primary кнопки единый стиль, цветовая палитра ограничена 5 цветами, spacing кратен 8px (8px grid)."
  },
  {
    question: "Что такое Design System?",
    options: [
      "CMS для сайта",
      "Набор Design Tokens, UI Components, Patterns и Guidelines для единообразного дизайна",
      "Инструмент для рисования",
      "База данных"
    ],
    correctAnswer: 1,
    explanation: "Design System — набор стандартов для единообразного дизайна. Компоненты: Design Tokens (цвета, шрифты, spacing), UI Components (Button, Input, Card), Patterns (Forms, Navigation, Modals), Guidelines (когда использовать что). Примеры: Material Design (Google), Apple Human Interface Guidelines, Fluent Design (Microsoft), Cloudscape (AWS)."
  },
  {
    question: "Что такое Visual Hierarchy?",
    options: [
      "Иерархия файлов",
      "Организация элементов по важности через размер, цвет, позицию, контраст",
      "Структура команды",
      "Иерархия БД"
    ],
    correctAnswer: 1,
    explanation: "Visual Hierarchy (визуальная иерархия) — организация элементов по важности. Принципы: Size (важное крупнее), Color (акценты для важного), Position (важное выше/левее — F-pattern), Contrast (важное контрастнее). Пример требования: Primary CTA (Call-to-Action) должен быть самой заметной кнопкой, заголовки H1 крупнее текста в 2 раза."
  },
  {
    question: "Что такое Affordance в UI?",
    options: [
      "Стоимость разработки",
      "Визуальные подсказки как использовать элемент (кнопка выглядит нажимаемой)",
      "Скорость работы",
      "Размер элемента"
    ],
    correctAnswer: 1,
    explanation: "Affordance — визуальные подсказки как использовать элемент. Примеры: кнопка выглядит 'нажимаемой' (тень, градиент), ссылка подчёркнута или цветная, input field имеет border, slider имеет ручку для перетаскивания. Good affordance → пользователь интуитивно понимает что делать без инструкций."
  },
  {
    question: "Где должен быть label у input поля в форме?",
    options: [
      "Внутри input (placeholder)",
      "Сверху input (top-aligned) — лучшая практика",
      "Справа от input",
      "Снизу input"
    ],
    correctAnswer: 1,
    explanation: "Label лучше сверху input (top-aligned): быстрее сканирование формы, label всегда виден (placeholder исчезает при вводе), лучше для длинных labels. Placeholder НЕ заменяет label — используется для примера формата. Плохо: label внутри (исчезает при вводе, пользователь забывает что вводил). Требование: required поля должны иметь звёздочку (*) в label."
  },
  {
    question: "Когда должна срабатывать inline валидация формы?",
    options: [
      "При submit формы",
      "onBlur (потеря фокуса поля) — лучшая практика",
      "При каждом символе",
      "Валидация не нужна"
    ],
    correctAnswer: 1,
    explanation: "Inline валидация лучше срабатывает onBlur (потеря фокуса) — пользователь заполнил поле, переходит к следующему → валидация. Не при каждом символе (раздражает, показывает ошибку пока пользователь вводит). Показывать: что правильно (checkmark зелёный), что не так (error красный). Error message конкретное: 'Email должен содержать @' вместо 'Неверный формат'."
  },
  {
    question: "Что лучше для loading states: spinner или skeleton screens?",
    options: [
      "Spinner всегда лучше",
      "Skeleton screens лучше — показывают структуру будущего контента",
      "Нет разницы",
      "Loading не нужен"
    ],
    correctAnswer: 1,
    explanation: "Skeleton screens лучше spinner — показывают структуру будущего контента (серые блоки на месте текста, изображений). Преимущества: perceived performance (кажется быстрее), пользователь понимает что загружается, снижает frustration. Spinner: только если неизвестна структура. Требование: операции > 1 секунды должны показывать loading indicator, загрузка списков использует skeleton screens."
  },
  {
    question: "Как правильно показывать Empty State (пустое состояние)?",
    options: [
      "Просто 'Нет данных'",
      "Объяснить почему пусто + CTA что делать дальше",
      "Ничего не показывать",
      "Показать spinner"
    ],
    correctAnswer: 1,
    explanation: "Empty State должен: 1) Объяснить почему пусто, 2) CTA что делать дальше. Плохо: 'Нет данных' (непонятно почему). Хорошо: 'Мы не нашли товары по запросу xyz. Попробуйте другие ключевые слова' или 'У вас ещё нет заказов. [Кнопка: Начать покупки]'. Делает UX дружелюбным, направляет пользователя."
  },
  {
    question: "Что такое System Usability Scale (SUS)?",
    options: [
      "Скорость системы",
      "Стандартный опросник 10 вопросов для измерения usability, шкала 0-100, цель > 68",
      "Размер системы",
      "Стоимость системы"
    ],
    correctAnswer: 1,
    explanation: "System Usability Scale (SUS) — стандартный опросник 10 вопросов для измерения usability. Шкала: 0-100, среднее: 68, хорошо: > 68, отлично: > 80. Вопросы типа: 'Я бы использовал систему регулярно', 'Система проста в использовании'. Преимущества: быстро (2 минуты), валидирован, сравнимо с другими продуктами. Требование: SUS > 68."
  },
  {
    question: "Что такое Net Promoter Score (NPS)?",
    options: [
      "Количество пользователей",
      "Метрика лояльности: 'Порекомендуете ли продукт?', шкала -100 до +100, цель > 50",
      "Скорость работы",
      "Прибыль компании"
    ],
    correctAnswer: 1,
    explanation: "Net Promoter Score (NPS) — метрика лояльности. Вопрос: 'Порекомендуете ли вы продукт?' Шкала 0-10. Promoters (9-10), Passives (7-8), Detractors (0-6). NPS = % Promoters - % Detractors. Диапазон: -100 до +100. Хорошо: > 0, отлично: > 50. Требование: NPS > 50. Показывает готовность пользователей рекомендовать продукт."
  },
  {
    question: "Сколько пользователей нужно для usability testing по Jakob Nielsen?",
    options: [
      "100 пользователей",
      "Минимум 5 пользователей найдут 85% проблем usability",
      "1 пользователь достаточно",
      "1000 пользователей"
    ],
    correctAnswer: 1,
    explanation: "По Jakob Nielsen, минимум 5 пользователей найдут 85% проблем usability. 1 пользователь находит ~30%, 2-3 пользователя ~50%, 5 пользователей ~85%. Больше 5 — diminishing returns (мало новых проблем). Лучше: 3-5 раундов по 5 пользователей (итеративное тестирование) чем 1 раунд 20 пользователей. Бюджет эффективнее."
  },
  {
    question: "Какой минимальный цветовой контраст текста по WCAG 2.1 AA?",
    options: [
      "Нет требований",
      "Normal text ≥ 4.5:1, Large text ≥ 3:1",
      "Всегда ≥ 10:1",
      "Всегда ≥ 2:1"
    ],
    correctAnswer: 1,
    explanation: "WCAG 2.1 уровень AA требования к цветовому контрасту: Normal text (< 18pt) ≥ 4.5:1, Large text (≥ 18pt или ≥ 14pt bold) ≥ 3:1. Пример: чёрный текст #000 на белом фоне #FFF = 21:1 (отлично). Серый #767676 на белом #FFF = 4.5:1 (минимум для normal text). Инструменты проверки: WebAIM Contrast Checker. Требование: NFR-14: цветовой контраст ≥ 4.5:1."
  },
  {
    question: "Что означает 'Don't make me think' — Steve Krug?",
    options: [
      "Пользователи не должны думать вообще",
      "Интерфейс должен быть интуитивным — пользователь сразу понимает что делать без раздумий",
      "Нужно упрощать код",
      "Нужно убрать все функции"
    ],
    correctAnswer: 1,
    explanation: "'Don't make me think' — Steve Krug (книга об usability) — интерфейс должен быть интуитивным, self-evident (самоочевидным). Пользователь сразу понимает что делать без раздумий, инструкций, поиска. Принципы: понятные labels, логичная navigation, следование конвенциям, good affordance. Если пользователь думает 'Где кнопка?', 'Что это значит?' → плохой UX."
  },
  {
    question: "Зачем нужен Think Aloud Protocol в usability testing?",
    options: [
      "Чтобы пользователь говорил громче",
      "Пользователь озвучивает свои мысли во время тестирования — понимаем что он думает, где застревает",
      "Чтобы пользователь читал вслух",
      "Чтобы записать голос"
    ],
    correctAnswer: 1,
    explanation: "Think Aloud Protocol — пользователь озвучивает свои мысли во время выполнения задачи ('Я ищу кнопку корзины', 'Непонятно что делать дальше', 'Ожидал увидеть здесь'). Цель: понять что пользователь думает, где затрудняется, что ожидает. Выявляет проблемы usability, которые не видны по действиям. Ценнейший инсайт для улучшения UX."
  }
];

async function createTest() {
  try {
    console.log('🚀 Создание вопросов для теста "Требования к UI/UX и Usability" (v2)...\n');

    const test = await prisma.test.findUnique({
      where: { id: 'cmkbleojd000caovn6b7u57s6' }
    });

    if (!test) {
      throw new Error('❌ Тест не найден!');
    }

    console.log('✅ Найден тест:', test.title);
    console.log(`📊 Текущее количество вопросов: ${await prisma.testQuestion.count({ where: { testId: test.id } })}\n`);

    console.log('📝 Создание лекции v2...');
    const lecture = await prisma.lecture.create({
      data: {
        title: 'Требования к UI/UX и Usability (v2)',
        topic: 'uiux-usability',
        content: lectureContent
      }
    });

    console.log(`✅ Лекция v2 создана: ${lecture.id}\n`);

    console.log('❓ Добавление вопросов...\n');

    let addedCount = 0;

    for (let i = 0; i < questions.length; i++) {
      const q = questions[i];

      const existing = await prisma.question.findFirst({
        where: { question: q.question }
      });

      if (existing) {
        console.log(`⚠️  [${i + 1}/${questions.length}] Вопрос уже существует, обновляем лекцию`);
        await prisma.question.update({
          where: { id: existing.id },
          data: { lectureId: lecture.id }
        });
        continue;
      }

      const newQuestion = await prisma.question.create({
        data: {
          question: q.question,
          options: q.options,
          correctAnswer: q.correctAnswer,
          explanation: q.explanation,
          lectureId: lecture.id
        }
      });

      await prisma.testQuestion.create({
        data: {
          testId: test.id,
          questionId: newQuestion.id,
          order: i + 1
        }
      });

      console.log(`✅ [${i + 1}/${questions.length}] ${q.question.substring(0, 70)}...`);
      addedCount++;
    }

    console.log(`\n🎉 Готово!`);
    console.log(`✅ Добавлено вопросов: ${addedCount}`);
    console.log(`📚 Лекция v2 ID: ${lecture.id}`);
    console.log(`📝 Тест ID: ${test.id}`);

    const finalCount = await prisma.testQuestion.count({ where: { testId: test.id } });
    console.log(`📊 Итого вопросов в тесте: ${finalCount}`);

  } catch (error) {
    console.error('❌ Ошибка:', error);
    throw error;
  } finally {
    await prisma.$disconnect();
    await pool.end();
  }
}

createTest();
